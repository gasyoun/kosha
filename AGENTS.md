# AGENTS.md — kosha

This file orients Codex and other non-Claude agent sessions. The section between the generated-block markers is machine-owned (regenerated from Uprava; hand edits there are overwritten) — add repo-curated content OUTSIDE the markers.

[generated-block: repo-context — do not hand-edit; regenerated by Uprava tools/gen_repo_context.py; content-hash: 03a02e89e8]
## Shared org context (generated — H1393 layer)

- Canonical Sanskrit-domain facts (encodings, key1/key2, corpus gaps, transcoder traps): read the [Sanskrit context primer](https://github.com/gasyoun/github-spine/blob/main/SANSKRIT_CONTEXT_PRIMER.md) before touching encodings, transliteration, or corpus data.
- Task routing: [CLAUDE.md](https://github.com/gasyoun/kosha/blob/main/CLAUDE.md) — open the matching section; do not read the file end-to-end. Sections: What This Repository Is · Common commands · Key directories / files · Conventions · What not to touch · Operational hazard notes.
- Danger facts (destroy-work-if-missed; full text + sources in [DANGER_FACTS.md](https://github.com/gasyoun/Uprava/blob/main/DANGER_FACTS.md)):
  - git worktree add on this large repo can time out at 2 min leaving an empty index (everything staged-deleted) + stale index.lock + partial checkout — rm lock, git reset HEAD, git checkout -- . ; remove worktrees with -f -f (H1366 incident)
  - Some manifest datasets carry restricted rights tiers — check data/manifest/datasets.json before copying bytes anywhere
  - deploy_guhya.py uploads whatever exists - no size>0 guard; on 23-08-2026 SamudraManthanam/web/corpus.db was a 0-byte stub while datasets.json claimed 655 MB, and 13 underscore renou MANIFEST names were deleted-on-disk ghosts. Verify per-target local size vs registered size before any manifest-driven backup run
  - pdftotext blanks Cyrillic while returning plausibly-shaped output - never poppler on Russian PDFs.
  - git pull / rebase --autostash in a shared clone can swallow another session's uncommitted work — prefer git fetch + a fresh worktree
  - Deleting opencode.db deletes all OpenCode chats - it is the session store, never treat as disposable.
- Further operational hazard notes live in the org-private hub ([Uprava DANGER_FACTS.md](https://github.com/gasyoun/Uprava/blob/main/DANGER_FACTS.md); org members only).
- Cross-repo data edges (from [Uprava interlinks_edges.tsv](https://github.com/gasyoun/Uprava/blob/main/interlinks_edges.tsv)):
  - vendors → `ext:vidyut`: inflection/lemmatization engine (dual-engine E1) (live)
  - feeds → `buhler-sanskrit-book`: per-lemma static dictionary cards (queued)
  - produces → `*`: data-v* releases: canonical derived-dataset serving tier (live)
  - produces → `*`: corpus_sandhi.tsv — corpus-attested sandhi teaching layer over DCS (manifest corpus-sandhi/gita-sandhi/sandhi-curriculum) (live)
  - feeds → `Systema-Sanscriticum`: cohort_start_chteniya (live)
  - ← from `MWinflect` (feeds): full MW inflection tables for form→lemma lookup (proposed)
  - ← from `csl-guides` (vendors): top-2,000 lemma_frequency excerpt → src/data/corpus-frequency.json (live)
  - ← from `csl-santam` (feeds): tamil SQLite corpus (mwd+cap+otl, 321,620 entries) fold (queued)
  - ← from `VisualDCS` (feeds): data/frequency/lemma_frequency.tsv (83,277 rows, M9 join) (live)
  - ← from `SanskritLexicography` (feeds): union_headwords + mw_roots + mw_etymology in data-v0.1.0 release (live)
  - ← from `Uprava` (consumes): samskrutam-crossword word selection from lemma_frequency.tsv (source of truth Uprava/crossword/) (live)
  - ← from `SanskritLexicography` (feeds): pwg-sense-attestation-window (PWG per-sense attestation window, Ceiling C2 phase 1) (live)
- Gitignored/off-git canonical assets are registered as pointer rows in the org-private [shadow-assets registry](https://github.com/gasyoun/Uprava/blob/main/SHADOW_ASSETS_POINTERS.md).

[/generated-block]

## Repo-curated notes (hand-maintained)

- **Salt compatibility boundary (H2768):** read
  [docs/DECISION_H2768_SALT_FACE_EXTENSION_CONTRACT.md](https://github.com/gasyoun/kosha/blob/main/docs/DECISION_H2768_SALT_FACE_EXTENSION_CONTRACT.md)
  before changing `/dicts/*`, `/api/v1`, or entry serialization. Strict Salt
  faces exclude `kosha`; kosha-owned faces retain it.
- **Sandhi methods (H2069):** for rule induction, method A/B/C decision table, and miss taxonomy read [docs/SANDHI_METHODS_DEEP_MANUAL.md](https://github.com/gasyoun/kosha/blob/main/docs/SANDHI_METHODS_DEEP_MANUAL.md). For inventory / pedagogy surfaces use [SANDHI_PROGRAMME.md](https://github.com/gasyoun/kosha/blob/main/SANDHI_PROGRAMME.md) — do not expand methods prose into the hub.
