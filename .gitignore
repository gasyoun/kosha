__pycache__/
*.pyc
.env
.env.deploy
# machine-local Claude Code settings, not a repo deliverable
.claude/settings.local.json
*.db
# personal review/voting sheets (per Uprava's /review-sheet convention) -- working
# artifacts for MG, not repo deliverables; decisions.json output lands here too
review/
*.sqlite
# W2A (H2346): committed mini-archive fixture for historical-resolution tests
# (tiny public MW-shaped sample; not bulk data/releases dumps).
!tests/fixtures/archives/
!tests/fixtures/archives/**
data/raw/
data/raw_sqlite/
data/releases/
# Built stores + their DAG sidecars: *.db is already ignored, but the build
# writes <target>.lock.json and a .build/ scratch dir next to it (H1944).
data/db/
# W1D local deployment bundles + last rehearsal transcript (H2344). Digests and
# payload copies are regenerable via scripts/assemble_deploy_bundle.py /
# scripts/rehearse_deploy.py; never commit host-specific bundle trees.
data/deploy_bundles/
# csl-sqlite-shaped cache materialized from the committed fixture pack by
# `kosha.build.fixtures` — regenerable, deterministic, never committed.
data/.fixture-cache/
# vidyut-cheda linguistic data (P4 K2a segmentation fallback) — vendored once
# via `python -c "import vidyut; vidyut.download_data('data/vidyut')"`, ~sizeable
# binary model + FSTs, regenerable; never committed (RISKS.md R12: local data,
# not a live call). Pointed at by KOSHA_VIDYUT_DATA.
data/vidyut/
# ...except the small vendored chandas meters table (5 KB, load-bearing for the W3a
# metre annotator scripts/build_reading_pack_metre.py; H951) — tracked intentionally.
!data/vidyut/chandas/
!data/vidyut/chandas/meters.tsv
# P2 static-cache generator output (scripts/build_static_cache.py) — regenerable
# from kosha.db; deployed to Pages by MG (A3), never committed in-repo.
.pytest_cache/
# P5 static word-page prerender (scripts/build_word_pages.py, H537) — one HTML
# per attested lemma + the /browse spine. ~478 MB at full N, 100% regenerable
# from the committed docs/cards/ set (the script needs no DB); MG regenerates at
# deploy like build_static_cache.py. Not committed (the source + cards are).
docs/w/
docs/browse/
# K2b inflection UI (ui/) — build toolchain deps + local vitest artifacts.
# The BUILT app (docs/inflect/) and the curated demo data shards ARE committed
# (they are the Pages deploy input, like docs/cards/); the full-set data from
# `build_paradigms.py --all` is deployed by MG out-of-band (A3), not committed.
ui/node_modules/
ui/dist/
ui/coverage/
# D5 measurement run outputs (machine-specific, regenerable via scripts/measure_d5.py)
data/d5_run*.log
data/d5_profile.log
data/d5_measurements.json
# E1 dual-engine comparison outputs (regenerable via scripts/compare_vidyut_cologne.py);
# the narrative lives in E1_DIVERGENCE_REPORT.md, the raw stats are regenerable.
# `dhatu_crosswalk.json` (H855) is the exception: a small COMMITTED run-time input
# so the verb comparison needs only bundled vidyut, not the external vidyut-data.
data/e1/e1_*
# E1 hybridize pass measurement outputs (regenerable via scripts/build_hybrid_forms.py).
data/hybrid/

# H1455 sense-reconciliation input (regenerable via RussianTranslation microstructure.py export_sense_loci; owned by H1456)
data/concordance/pwg_sense_loci.tsv

# H1455 wave-1.5 MBh crosswalk (consumed from csl-atlas f8_mbh_resolve.py / mbh_citation_census; regenerable)
data/concordance/mbh_vulgate_concordance.csv

# H1588 W3a — SCL Reading-Aid minimal sense-label cache (rights-gated, H057).
# Labels only; never commit SCL/GPL body text, HTML, or gloss dumps (ruling N4).
data/frequency/.cache/

data/raw_akshara_pilot/
data/akshara_pilot/parsed_corpus.jsonl
data/akshara_pilot/bench/judge_packets/
data/akshara_pilot/bench/bench_items.jsonl
data/akshara_pilot/bench/tm_snapshot.jsonl

# H3457 word-page UX STAGING output (scripts/build_word_pages.py --ux-staging) —
# local-only artifact tree, never the Pages input; docs/NOT_PUBLISHED_H3457_WPAGE_UX.md
dist/
# H3457 mockup screenshots are committed (design packet evidence) — nothing to ignore
