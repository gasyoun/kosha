# Project Objective: Ship kosha as a reproducible, secure, Cologne-compatible public dictionary/API with durable citations

## ➡️ Next Steps (Queue)

1. **H1944 — W0B reproducible substrate (Opus 4.8, `claude-opus-4-8`).** Dependency: H1943
   merged and Codex retrospective has no P0/P1. Do not claim early.
2. **H1945 — W0C contract and trust boundaries (Opus 4.8, `claude-opus-4-8`).** Dependency:
   H1944 merged and Codex retrospective has no P0/P1. Do not claim early.
3. W1 public readiness, W2 citable v1, and W3 sense reconciliation remain
   gated by the W0 freeze-exit checklist.

## 🚧 Current Work-In-Progress (WIP)

(none — H1943 shipped; H1944 not yet claimed)

## 🧠 Dev Notes & Hypotheses (Bugs, ideas, context)

- H1943 audit result: Python 389 passed / 2 skipped (0 failing); UI 42 passed
  + build green; manifest 96 datasets (81 public · 11 restricted · 4
  intermediate), README/directory counts now match.
- **Confirmed, deferred to H1944/#210 (out of H1943 scope — non-goal:
  database/DAG):** `build_db.py`'s no-flag default build intentionally skips
  the `entries`/`forms`/`inflections`/`hybrid`/`stem_bridge` stages (each
  documented as opt-in in `scripts/build_db.py`'s stage table, e.g.
  `inflections` needs the sibling MWinflect checkout). A fresh clone running
  the bare `python scripts/build_db.py` command from the README does not
  reproduce a fully-queryable DB — H1944 owns making that reproducible
  (immutable build-lock, postconditions, atomic promotion).
- Other W0 blockers (H1944/H1945 scope): missing required Python/UI CI (two
  workflows exist — changelog-lint, dependabot-auto-merge — neither runs the
  test suite yet), Salt-contract drift, duplicated API/static serialization,
  unsafe rendered HTML boundary, disabled-by-ruling history/auth, and
  incomplete citation archive/deployment contract.
- Rights policy is deliberately broad and risk-tolerant: uncertainty does not
  delay work or publication. Only confirmed prohibition, explicit restricted
  tier, privacy exposure, or platform policy blocks.
- Hard feature freeze remains active until H1943–H1945 merge and the
  verification freeze-exit checklist passes.

## ✅ Completed (Recent only)

- 30-07-2026 — repository, GitHub, prior-art, architecture, roadmap, quality,
  security, and verification audit completed by Codex (`gpt-5.6-sol`).
- 30-07-2026 — four-round `/ask` interview completed; 23 user rulings recorded.
- 30-07-2026 — H1943–H1945 atomically minted for Sonnet/Opus Claude Code
  execution; no Codex implementation handoff exists.
- 31-07-2026 — **H1943 W0A governance/integrity shipped** (Sonnet 5,
  `claude-sonnet-5`): fixed the two `#198` false "Gita absent from DCS"
  manifest mirrors; regenerated `directory/index.html` + README dataset
  counts (94→96) via the committed builder; established
  [docs/ROADMAP.md](https://github.com/gasyoun/kosha/blob/main/docs/ROADMAP.md)
  as the sole live roadmap; pointed root `ARCHITECTURE.md` at the platform
  contract while preserving A1–A4; bannered 4 superseded portfolio-level
  plan/roadmap docs SUPERSEDED in place; corrected the stale
  pre-alpha/no-CI/no-lookup claims in `CLAUDE.md`; added 5 fail-fast truth
  tests (`tests/test_truth_gates.py`) for version claims, active-queue
  completed markers, and required plan banners; full Python (389 passed) +
  UI (42 passed, build green) gates confirmed clean.

---

_Dr. Mārcis Gasūns_
