# Project Objective: Ship kosha as a reproducible, secure, Cologne-compatible public dictionary/API with durable citations

## ➡️ Next Steps (Queue)

0. **Owed after H3925: narrow `form_key()`'s MEDIAL anusvāra fold before a labial.** The
   0.11.0 fix collided word-final `-ṃ` with `-m`; medial anusvāra still folds to `n`
   everywhere, which is right before most consonants and wrong before `p b m`, where it is
   phonetically /m/. Measured cost on the A3 give-back: **278 of 2,521 `slot-conflict` rows
   (11.03%, 11.58% by corpus weight) are not disagreements at all** — `saṃbhavaḥ` vs
   `sambhavaḥ`, `vaiśaṃpāyana` vs `vaiśampāyanaḥ` — plus 90 candidates that would leave A¬G
   outright. Re-measure with
   [`scripts/measure_medial_anusvara_residual.py`](https://github.com/gasyoun/kosha/blob/main/scripts/measure_medial_anusvara_residual.py).
   **Not a kosha-local change:** it is a third edit to `sanskrit-util`, which ~85 repos
   consume, so it needs its own release, the WhitneyRoots donor sync, a golden-vector
   regeneration and then another full A3 chain re-run via `scripts/rebuild_a3_chain.py`
   (~25 min). Until it lands the affected rows are screened off the human validation sheet
   by a named rule, so nothing incorrect reaches a reviewer or csl-inflect.
1. **H2670 / H2671 / H2672 / H2680 / H2681 shipped.** The 13-08 agent
   handoffs are complete. Index:
   [docs/PLAN_KOSHA_RU_LANG_GROUPS_OPS_2026.md](https://github.com/gasyoun/kosha/blob/main/docs/PLAN_KOSHA_RU_LANG_GROUPS_OPS_2026.md).
2. **W1E agent residuals closed.** Archive mount = H2671; identity
   rollback = H2672. The W1E packet Sign-off branded-complete box is
   **not** a legal signature and **not** a stop for further engineering.
   An agent still must **not** declare Wave 1 product-exit complete.
3. The H2681 retrospective found no open P0/P1, so the technical W0 review
   criterion now passes. W2 citable v1 and W3 sense reconciliation remain
   governed by their roadmap and human/product gates; this is not a Wave 1
   product-exit claim.

**Note — `main` is branch-protected since 31-07-2026** (`enforce_admins: true`,
both CI workflows required by name). *Direct pushes to `main` are refused for
everyone, including release commits* — cut releases through a PR. Configuration
and per-setting reasoning:
[VERIFICATION_KOSHA_ARCHITECTURE.md](https://github.com/gasyoun/kosha/blob/main/docs/VERIFICATION_KOSHA_ARCHITECTURE.md)
§ Required checks.

## 🚧 Current Work-In-Progress (WIP)

- **H3597 (OxAlpha, 27-08-2026) - akshara.ru FULL kosha crawl RUNNING, self-healing, 2 streams; INCIDENT (case-twin collision) repaired 28-08, repair pass due at drain.** Census
  FROZEN pre-fetch: 51,663 heads. Crawler defaults to `--workers 2`
  (per-connection politeness unchanged; measured 0.69 URLs/s; full-crawl ETA
  ~1 Sept). Supervised by scheduled task
  `kosha-akshara-crawl-watchdog` (10-min ticks, StartWhenAvailable + WakeToRun,
  self-disables when both passes done/exhausted; kill-test proven 28-08). Crawler holds Windows keep-awake while alive and
  validates `data-q-slp1` parity inline. **Incident 28-08:** flat raw filenames collided on
  case-insensitive NTFS for case twins the site serves as different cards (`dvipAd` vs
  `dvipad`; 5,175 case-variant census keys) - filenames now carry a sha1-8 suffix; parse
  (`akshara_full_parse.py --delete-raw`, wisdomlib-shaped JSONL storage) landed 33,436
  rows + freed ~1.8 GB; repair pass over ~7.1k twin keys is part of drain (sequence in
  report §5b + GTD row). Session worktree `kosha-h3597-14872` — **do not GC that
  worktree until drain** and do not unregister the task before drain. Drain checklist
  (§5/§5b/§8/§9):
  [docs/AKSHARA_FULL_COVERAGE_H3597_27.08.26.md](https://github.com/gasyoun/kosha/blob/main/docs/AKSHARA_FULL_COVERAGE_H3597_27.08.26.md).
  Uprava GTD @DO row tracks the drain.

(none)

## ✅ Completed (Recent only)

- **H3783 (02-09-2026, Opus 5 `claude-opus-5`) — closed DUPLICATE-SHIPPED; the
  A4 exit-check prose that minted it is corrected.** All three exit checks of
  concordance axis A4 were already on `main`: the sūtra-coverage map (W3a,
  24-07-2026, H1468, [PR #180](https://github.com/gasyoun/kosha/pull/180) —
  [`sutra_coverage_map.tsv`](https://github.com/gasyoun/kosha/blob/main/data/concordance/sutra_coverage_map.tsv)
  over the **named** vidyut 0.4.0 enumeration, n = 3983: `lit` 221 ·
  `dark-unattested` 55 · `dark-out-of-scope` 3707 · `dark-engine-gap` 0), the
  30-chain derivation sample (W2a, 20-07-2026, H1368), and the
  derivation-metadata licence (18-07-2026, H1263 — vidyut code **and** data MIT
  verified separately, DCS CC BY 4.0 from Hellwig's own terms, A4 ships
  CC BY-SA 4.0, and the record's own § *Human gate — not triggered* says why no
  `@DECIDE` is owed), all released as
  [data-v0.3.0](https://github.com/gasyoun/kosha/releases/tag/data-v0.3.0) on
  24-07-2026. What was stale was
  [CONCORDANCE_ROADMAP.md](https://github.com/gasyoun/kosha/blob/main/CONCORDANCE_ROADMAP.md)'s
  Q4 **exit-check bullet** and **open `@DECIDE` 1** — the H3782 truth-pass of
  01-09 corrected the section's calendar slot and status table but not these,
  and they are the sentences a `/fruit` or `/ask` pass quotes
  ([Uprava FINDINGS §644](https://github.com/gasyoun/Uprava/blob/main/FINDINGS.md)
  class). Both fixed with per-check evidence in
  [PR #487](https://github.com/gasyoun/kosha/pull/487), released as
  [v0.117.4](https://github.com/gasyoun/kosha/releases/tag/v0.117.4). **Open
  residual, human-only:** the 30 chains carry no recorded verdict and no
  justification for N = 30 — an MG `@DO` row in
  [Uprava GTD](https://github.com/gasyoun/Uprava/blob/main/GTD_NEXT_ACTIONS.md).

- **H3788 (01-09-2026, Opus 5 `claude-opus-5`) — W2 citable v1 established and
  recorded; four citability defects repaired.** A citable release already
  existed — `v0.117.1`, version DOI
  [`10.5281/zenodo.22231444`](https://doi.org/10.5281/zenodo.22231444) under
  concept DOI
  [`10.5281/zenodo.21965599`](https://doi.org/10.5281/zenodo.21965599), 21
  deposits since the 14-08-2026 webhook — and **neither W0 nor W1 blocks it**
  (W0 freeze-exit criteria all met; every agent-measurable W1 gate PASSES, the
  only open item being the human §9 branded-complete tick, which the W1E packet
  itself records as not a stop). Missing were the record and the **stated
  versioning policy**, the fourth W2 component:
  [docs/VERSIONING_AND_CITATION_POLICY_KOSHA.md](https://github.com/gasyoun/kosha/blob/main/docs/VERSIONING_AND_CITATION_POLICY_KOSHA.md)
  + [docs/CITABLE_V1_RECORD_KOSHA_01.09.26.md](https://github.com/gasyoun/kosha/blob/main/docs/CITABLE_V1_RECORD_KOSHA_01.09.26.md).
  **Read the policy before cutting any data release.** Four defects:
  (1) `.gitattributes` pins `*.tsv eol=lf` but git never renormalizes an
  existing working tree, so **28 manifest rows** carried CRLF-inflated
  `size_bytes` across three DOI-archived releases and the published
  `data-v0.5.0` asset is the CRLF variant — the canonical checkout still needs
  `git add --renormalize .` (GTD row); (2) tag `v0.116.0` was moved and carries
  **two** version DOIs — cite `10.5281/zenodo.22132415`, never move a released
  tag; (3) `update_manifest.py` resolved siblings as `REPO.parent`, blind inside
  a worktree — fixed via
  [scripts/manifest_paths.py](https://github.com/gasyoun/kosha/blob/main/scripts/manifest_paths.py),
  which immediately exposed a shrink hazard (`pwg-tm-canonical-v1`
  101,677,729 → 17,668 B, payload ships outside git), now blocked by a
  **50 % shrink floor** (`--allow-shrink` to override); (4) stale `generated`,
  `interim_release`, `CITATION.cff` comment and DOI checklist. New
  [scripts/freeze_release_manifest.py](https://github.com/gasyoun/kosha/blob/main/scripts/freeze_release_manifest.py)
  makes the frozen release manifest a build product — LF-canonical digests, a
  **public-tier fence that refuses rather than dropping** a restricted row, a
  CRLF-checkout refusal, and a `check` mode for the release gate; frozen
  manifests committed for all four published data releases. [PR #481](https://github.com/gasyoun/kosha/pull/481).
  Org lessons: Uprava FINDINGS §654 / §655.

- **H3862 (02-09-2026, Opus 5 `claude-opus-5`):** sense-reconciliation
  wave-2 slice 2 — **ŚKDR and VCP as further columns** on the aligned-sense
  table, `shape` extended from `pwg-mw-apte` to five positions.
  [PR #492](https://github.com/gasyoun/kosha/pull/492), released
  [v0.117.6](https://github.com/gasyoun/kosha/releases/tag/v0.117.6).
  **Two of the four dictionaries asked for do not exist as CDSL sources**
  and ship as recorded absences: **Medinī is not in CDSL** — the `md` code
  in csl-orig/csl-sqlite is **Macdonell** 1893 — and neither is Amara.
  **The kośas carry zero `<ls>`** (0 of 42,531 ŚKDR and 0 of 50,135 VCP
  records), so the shared-witness bridge does not exist for them and the
  gloss bridge is shut by the language fence. What ships is the reverse
  direction: PWG's own citations *of* them (`ŚKDR.` 1,227×, `MED.` 1,824×
  on the pilot) as the new `attrib` method — weighted `1/df` off the same
  table, decided by the same τ, **no new constant**, ranked below `ls`
  because it is one-directional. Abbreviations reach it through an explicit
  `ATTRIB_KEYS` table; `PREFIX_MIN` stays at 4. The language fence is now
  declared as data (`GLOSS_LANG` + `gloss_channel_open()`), so Sanskrit is
  fenced off wording-comparison explicitly, as German was in slice 1.
  **Slice 1's numbers are unchanged, verified row by row against
  `--no-sasa`:** 0 rows appear the baseline lacked, the 67 that changed are
  exactly the 67 that gained a kośa cell, clean `1-1-1` still **262**;
  aligned 2,957 → 3,013 = 56 kośa-only meanings + 11 existing ones extended.
  New failure class `no-citation-apparatus` (1,832). An open-vocabulary
  `iti X` reading of the kośas' prose was measured and **rejected** (it
  matched `PRAT.` to `pratyarthin`). Publication fence untouched; smoke
  31/31 ([docs/H3862_SENSE_ALIGNMENT_SMOKE_LOG_02.09.26.md](https://github.com/gasyoun/kosha/blob/main/docs/H3862_SENSE_ALIGNMENT_SMOKE_LOG_02.09.26.md)),
  suite 606 passed. Org lesson: SanskritLexicography FINDINGS §624.
  **Sources note for the next session:** ŚKDR/VCP are read straight from
  `data/raw_sqlite/<d>/<d>.sqlite` (gitignored, fetched by
  `build_entries.fetch_release_sqlite`) and are deliberately **not** loaded
  into `kosha.db`.

- **H3744 (31-08-2026, Opus 5 `claude-opus-5`):** sense-reconciliation
  wave-2 slice 1 — the PWG·MW·Apte **aligned-sense table**, staged, with a
  published before/after compare page
  ([gasyoun.github.io/h3744-sense-align/](https://gasyoun.github.io/h3744-sense-align/)).
  New [app/sense_align.py](https://github.com/gasyoun/kosha/blob/main/app/sense_align.py)
  (row model + algorithm + failure taxonomy, DB-free, 26 unit tests) and
  [scripts/build_sense_alignment.py](https://github.com/gasyoun/kosha/blob/main/scripts/build_sense_alignment.py).
  Senses join a MEANING through shared `<ls>` literary witness weighted
  `1/df` within the lemma — the only signal that crosses PWG's German
  gloss boundary; gloss Jaccard is fenced to MW↔Apte. Grouping is greedy
  best-match per dictionary pair, **not** transitive closure (closure
  merged `amṛta`'s *not dead* / *nectar* / *N. pr. Mutter von Parikṣit*
  into one row). नागदन्त splits correctly: tusk↔`MBh.`, peg↔`Pañc.`.
  Pilot 500 heads: 30,470 rows, 2,957 aligned (9.7 %), 262 clean `1-1-1`,
  477/500 lemmas with ≥1 aligned meaning; unaligned senses kept as rows
  with a failure class, and a worked false positive documented.
  **Not on the live pages** —
  [docs/NOT_PUBLISHED_H3744_SENSE_ALIGNMENT.md](https://github.com/gasyoun/kosha/blob/main/docs/NOT_PUBLISHED_H3744_SENSE_ALIGNMENT.md);
  the organ rides an explicit `ux={"sense_align": True}` key, because
  plain `ux` truthiness stopped being a non-publication gate when H3457
  was published on 26-08-2026 (that stale claim in
  `app/word_page_ux.py`'s docstring was corrected in the same pass).
  Packet:
  [docs/H3744_SENSE_ALIGNMENT_PACKET_31.08.26.md](https://github.com/gasyoun/kosha/blob/main/docs/H3744_SENSE_ALIGNMENT_PACKET_31.08.26.md).
  Next slice, in order: Sa→Sa columns (ŚKDR/Medinī/VCP/Amara) · the
  acceptance pass (sample + judge + `/review-sheet` vote — until it runs,
  precision is unknown and no figure is quoted) · the lemma-variant graph.

- **H3479 (25-08-2026, Sonnet 5 `claude-sonnet-5`):** H3457 wave 2 —
  literary-source `<ls>` citation links on PWG entries. New
  [app/ls_hydrate.py](https://github.com/gasyoun/kosha/blob/main/app/ls_hydrate.py)
  hydrates `<span class='ls'>` into a link when the
  SanskritLexicography `ls_resolver.generate_href` port resolves it,
  classed `ls-scan` (csl-observatory `pwg_scan_index.tsv` `scan_wired`
  campaign row) or `ls-etext` (resolved but not campaign-tracked — no
  new resolver). Still `ux=`-gated, PWG only, **staged, not published**
  — same non-publication contract as H3457/H3480. Census 6,303
  citations over the 11-lemma sample (1,731 scan-wired, 3,569 e-text,
  737 mintable, 266 no-locus); smoke 22/22; 12/12 links live-verified.
  Packet:
  [docs/H3479_LS_CITATION_WAVE2_PACKET_25.08.26.md](https://github.com/gasyoun/kosha/blob/main/docs/H3479_LS_CITATION_WAVE2_PACKET_25.08.26.md).
  Drive-by: `CITATION.cff`'s "Zenodo not wired" note was stale — the
  webhook has archived releases since 14-08-2026 (concept DOI
  `10.5281/zenodo.21965599`).
- **H3490 (25-08-2026, Fable 5 `claude-fable-5`):** RU tab bare-SLP1
  quotations transliterated on the page (`ru_bare_slp1_pass`) + escaped-tag
  rows fixed + committed `w/` regenerated (2,324 pages, 74.2 MB) so the live
  site changed.
- **H3166 (Opus 5, `claude-opus-5`) — E1 verb residue adjudicated; the handoff's build was already shipped, 25-08-2026.**
  Report section: [E1_DIVERGENCE_REPORT.md](https://github.com/gasyoun/kosha/blob/main/E1_DIVERGENCE_REPORT.md)
  § "The 11,056 genuine conflicts are mostly NOT divergence"; released v0.113.0.
  **Do not re-derive:** (1) the dhātu-identity crosswalk **exists** — H855 shipped it
  14-07-2026 (`scripts/build_dhatu_crosswalk.py` → `data/e1/dhatu_crosswalk.json`,
  12.68 % → 70.24 %); H3166 was minted off a stale roadmap bullet and closed
  DUPLICATE-SHIPPED for that half; (2) the report's old sentence calling the 11,056
  residual cells "real differences needing scholarly review" is **retracted** —
  **85.0 % (9,396) belong to roots still seeded with a BARE Cologne root** (212 of 779
  crosswalk entries), so vidyut eats the root-final consonant (`yat` → `yyate`);
  (3) the discriminating probe is `scripts/probe_passive_conflict.py` — crosswalk seed
  vs bare root give the SAME malformation for `yat`/`kam`/`paṇ`/`ruc`/`kṣam`, while
  `pā` (actually resolved) gives the correct `pīyate` against Cologne's `pāyate`;
  (4) true divergence is at most **~1,660 cells / 3.5 %**, not 23 %; (5) the next rung
  is mechanical — resolve the 212 bare-seeded entries, 57 `unresolved` first — and is
  **not** done here; (6) `data/e1/e1_verbs_divergence.json` is gitignored: regenerate
  with `python scripts/compare_vidyut_verbs.py --db <path-to-kosha.db>`, and pass
  `--examples 20000` or the per-class lists truncate at 50 and every population number
  above becomes wrong; (7) the csl-inflect#8 give-back is drafted in the H3166 handoff
  body and is **NOT posted** (RELATIONS.md §2/§7).

- **H3480 (25-08-2026, Fable 5 `claude-fable-5`):** word-page header rethink
  after MG's review (R1–R5) — direction **d** staged (3 header rows, flat tab
  row, Gloss/Full switch) + public fix for raw `{#…#}`/`{%…%}` in the RU tab.
  Packet §7 in
  [docs/H3457_WPAGE_UX_STAGING_PACKET_25.08.26.md](https://github.com/gasyoun/kosha/blob/main/docs/H3457_WPAGE_UX_STAGING_PACKET_25.08.26.md).
- **H3457 (25-08-2026, Fable 5 `claude-fable-5`):** word-page UX layer
  **staged, not published** — study badge (`core_rank`), localStorage
  favorites + `favorites.html`, print-scan anchors with the PWG volume fixed
  through a new committed L→vol/col table (`pwg-print-anchors`). Winner
  direction **a** of three; `render_word_page(ux=None)` unchanged. Packet:
  [docs/H3457_WPAGE_UX_STAGING_PACKET_25.08.26.md](https://github.com/gasyoun/kosha/blob/main/docs/H3457_WPAGE_UX_STAGING_PACKET_25.08.26.md);
  flip-live gate:
  [docs/NOT_PUBLISHED_H3457_WPAGE_UX.md](https://github.com/gasyoun/kosha/blob/main/docs/NOT_PUBLISHED_H3457_WPAGE_UX.md).
  Side finding filed: [#433](https://github.com/gasyoun/kosha/issues/433).
- **H2870 (17-08-2026, Opus 5 `claude-opus-5`):** W2 citable-v1 agent leg. W2
  enumeration: W2A immutable archives ✅ H2346, W2B dataset API ✅ H2347, W2C
  observability ✅ H2348, DOI = **MG human gate**. The only ungated engineering
  left was W2A's own release gate, which shipped as a script no release path
  ever ran and which passed on `http://localhost:8000` — the development
  default — because `validate_public_base` only rejects the deployment host
  (R5), never a base that resolves nowhere but the machine that cut the
  freeze (R1). Gate now runs as a step of the required `Python CI` check
  against the committed prior+current mini-archive, and
  `validate_durable_public_base` rejects loopback/private/link-local/`.local`/
  single-label hosts on the **release** path only — runtime `GET /ready` still
  reports a localhost developer as ready. No Wave-1 or product-exit claim.
- **H2768 (16-08-2026, Codex Sol `gpt-5.6-sol`):** chose the strict Salt
  face: `/dicts/*` emits the six C-SALT fields plus `csl`; `/api/v1`, cards,
  and SSR retain `kosha`. One serializer plus a terminal projection; independent
  profile-derived wire-key regression; governing
  [decision record](https://github.com/gasyoun/kosha/blob/main/docs/DECISION_H2768_SALT_FACE_EXTENSION_CONTRACT.md). Delivery
  [PR #404](https://github.com/gasyoun/kosha/pull/404); release promotion
  [PR #405](https://github.com/gasyoun/kosha/pull/405) (`v0.110.13`). Gates:
  targeted 29 passed; full Python 572 passed / 165 skipped; UI 42 passed and
  build green; fixture DAG built twice.

- **H2165 (16-08-2026, Sonnet 5 `claude-sonnet-5`):** `data/nala_subhashita/`
  freeze — nala-1/2/3 + subhashita-beginner pinned with sha256 MANIFEST.json,
  `scripts/freeze_nala_subhashita.py --check` green. Decision: fresh manifest
  under `data/nala_subhashita/` rather than extending H2109's
  `cohort_start_chteniya/MANIFEST.json` — nala+subhashita is a distinct
  course from the "Старт чтения" cohort (hitopadesa-0 + sandhi L1-3), so a
  separate freeze keeps the two courses independently verifiable;
  subhashita-beginner is pinned again here (same bytes as H2109's pin) so
  this course's manifest is self-contained. H2109's freeze is untouched.
  Unblocks H2168 reader wiring.
- **H2681 (14-08-2026, Codex Sol `gpt-5.6-sol`):** written retrospective of
  H1944/#215 and H1945/#224 against their handoffs plus the independent H1944
  lane; no open P0/P1, one P2 routed to H2768 (Codex) — Resolve kosha extension
  on strict Salt compatibility faces. Memo:
  [docs/REVIEW_KOSHA_W0_H1944_H1945_CODEX_2026.md](https://github.com/gasyoun/kosha/blob/main/docs/REVIEW_KOSHA_W0_H1944_H1945_CODEX_2026.md);
  review comments on [#215](https://github.com/gasyoun/kosha/pull/215#issuecomment-5297808328)
  and [#224](https://github.com/gasyoun/kosha/pull/224#issuecomment-5297808587);
  delivery [PR #399](https://github.com/gasyoun/kosha/pull/399). Focused suite:
  188 passed / 30 skipped.
- **H2680 (14-08-2026, Grok 4.6 `grok-4.6`):** SanskritRussian public-tier
  gloss strip on `/w/` — one line under the headword, not a tab. Fixture
  hit `BU` / miss `zzqxw`. v0.110.12.
- **`/ask` RU language groups + v0.110.8 (13-08-2026, Grok 4.6 `grok-4.6`):**
  five-layer plan + metadoc; R1–R24 voted; H2670–H2672 rewritten to votes;
  H2680 (Grok 4.6 strip) and H2681 (Codex W0 memo) minted. R24: starters
  only, do not execute H2670 this sitting. Plan shipped
  [PR #389](https://github.com/gasyoun/kosha/pull/389).
- **H2665 (13-08-2026, Grok 4.6 `grok-4.6`):** Pages pack-token `/w/`
  heads — 2,324 committed HTML pages at repo-root `w/`; Meyer vs `/w/BU`
  live delta in COMPARISON.md.
- **v0.110.6 (13-08-2026, Grok 4.6 `grok-4.6`):** `/cut-release` of the
  H2653 All-tab Unreleased bullet. Live at
  [samskrtam.ru/w/BU](https://samskrtam.ru/w/BU) (hard-refresh).
- **H2653 (13-08-2026, Grok 4.6 `grok-4.6`):** word-page **All** tab —
  MW+PWG+AP90 stacked on `/w/{slp1}`;
  [PR #384](https://github.com/gasyoun/kosha/pull/384).
- **H2646 (13-08-2026, Grok 4.6 `grok-4.6`):** branded `samskrtam.ru`
  `/health` `/ready` `/metrics` `/api` `/w` proxy to kosha on `.92`
  (LAN `:8002`). WP home + `/faq/` unchanged. v0.110.5.
- **H2642 (13-08-2026, Grok 4.6 `grok-4.6`):** W1E packet re-fill after
  promoting sslip to v0.110.3 — `/metrics` + `X-Request-ID` live; Gītā
  1.1 13/13. Branded later closed by H2646; Pages/archive/§9 still residual.
- **H2348 (13-08-2026, Grok 4.6 `grok-4.6`):** W2C release observability —
  `X-Request-ID` + `GET /metrics` (low-cardinality, H2343-aligned
  readiness gauges); [`docs/RELEASE_OBSERVABILITY.md`](https://github.com/gasyoun/kosha/blob/main/docs/RELEASE_OBSERVABILITY.md).
  W2 agent lane closed; W3 not minted.
- **H2347 (13-08-2026, Grok 4.6 `grok-4.6`):** W2B / P-D6 public dataset catalog
  API — `GET /api/v1/datasets` + `GET /api/v1/datasets/{id}`; restricted → 404;
  surface `api-v1-datasets`; [PR #371](https://github.com/gasyoun/kosha/pull/371).
- **H2345 (08-08-2026, Grok 4.5 `grok-4.5`):** W1E MG live-smoke packet committed
  (`docs/MG_LIVE_SMOKE_PACKET_W1E.md`) — agent half only; no prod deploy.
- **H2344 (08-08-2026, Grok 4.5 `grok-4.5`):** W1D deploy bundle + runbook + local
  rehearsal + rollback; [PR #257](https://github.com/gasyoun/kosha/pull/257), v0.103.0.
- **H2315 (07-08-2026, Sonnet 5 `claude-sonnet-5`):** org EOL census fix — renormalized
  `docs/inflect/index.html` to LF, released [v0.99.0](https://github.com/gasyoun/kosha/releases/tag/v0.99.0).
  [PR #246](https://github.com/gasyoun/kosha/pull/246) fixed the 13 original CR bytes;
  the squash-merge itself reintroduced one CRLF line, fixed in
  [PR #247](https://github.com/gasyoun/kosha/pull/247). **Both merged via a temporary
  `enforce_admins` toggle (immediately restored)** — kosha's GitHub Actions has no
  billing/minutes, so `Fixture build + tests` and `vitest + vite build` queue forever
  and can never self-clear. This is a standing blocker for every future PR here until
  Actions billing is funded, not specific to this fix.
- **H2129 (02-08-2026, Sonnet 5 `claude-sonnet-5`):** dual-run compare of H2109 — independent re-derivation found the merged Grok lane (#232) had stale sha256/bytes for `sandhi-drills-l1-l3` and `sandhi-curriculum-l1-l3` (hashed pre-`.gitattributes`-normalization, so `--check` FAILED on a pristine clone despite release notes claiming green). Fixed in [#235](https://github.com/gasyoun/kosha/pull/235), builder now pins `newline="\n"` so it can't recur, released as [v0.98.5](https://github.com/gasyoun/kosha/releases/tag/v0.98.5). Compare memo: [Uprava H2129 archive](https://github.com/gasyoun/Uprava/blob/main/handoffs/archive/H2129-Sonnet_kosha_h2109-grok-dual-run-compare_01.08.26.md).
- **H1872 (02-08-2026, Fable 5 `claude-fable-5`):** A55/A56 JOHD data papers upgraded to submission drafts (readiness 2/5 → 4/5) — full JOHD section structure, all counts re-verified against frozen `data-v0.1.0` assets, A56 token-frequency distribution computed (6 tokens = 50%, 154 = 99%), A55 `n_dicts` table + witness-independence caveat integrated. DOI mint + submission remain human steps.
- **H2109 (01-08-2026, Grok 4.5 override dual-run):** `data/cohort_start_chteniya/` freeze — hitopadesa-0 + subhashita-beginner + sandhi L1–3 + MANIFEST sha256 + `scripts/freeze_cohort_start_chteniya.py --check` green (later found to have 2 stale hashes — see H2129 above); datasets row `cohort-start-chteniya-pack-freeze`.

## 🧠 Dev Notes & Hypotheses (Bugs, ideas, context)

- H1943 audit result: Python 389 passed / 2 skipped (0 failing); UI 42 passed
  + build green; manifest 96 datasets (81 public · 11 restricted · 4
  intermediate), README/directory counts now match.
- **#210 closed by H1944.** The no-flag build now expands the declared graph in
  `src/kosha/build/stages.py` and runs all ten stages. Optional stages whose
  feed or local library is genuinely absent (MWinflect, vidyut) are *skipped
  with a recorded reason* in the run log, the lock file, and
  `meta.build_stage_manifest` — the distinction that was missing before is
  between "not run because unavailable" and "not run because nobody wired the
  `if`".
- **Fixture profile is the cheap reproduction.** `--profile fixture` builds the
  whole graph from `tests/fixtures/build/sources/` in ~2 s with no sibling
  repos and no network; that is what CI runs. The full-data build still needs
  the five sibling checkouts.
- **All four H1945-scope blockers are closed** (Salt-contract drift, duplicated
  API/static serialization, unsafe rendered-HTML boundary, incomplete citation
  archive/deployment contract). What remains is not code — see the queue.
- **Three defects were found by the new gates, not by inspection.**
  `/api/v1/sense` rendered both its live and its archived branch without the
  sanitizer, so the entire boundary could be walked around by asking for a
  sense instead of a lemma; Starlette's routing 404/405 escaped error
  normalization because handlers bound to FastAPI's `HTTPException` never see
  the base class the router raises; and the static-card builder's "keep the two
  in lockstep" copy of the serializer had already drifted (it never implemented
  the `raw` branch it accepted a flag for). Writing the parity/sanitizer tests
  first is what surfaced all three.
- **A Salt `id` is dictionary-scoped.** `lemma-agni` is minted by MW, PWG *and*
  Apte; C-SALT never notices because its responses are single-dictionary, but
  kosha's lemma card merges three. The key on a card is `(kosha.dict, id)`.
- **`app/` is now shims for five modules** (`render`, `cite`, `scan_resolver`,
  `evidence`, `transliterate`), which live in the installed package so the
  serializer can import them without a `sys.path` insert. Bare-name imports
  still work (D11); new code should import `kosha.<name>`.
- Rights policy is deliberately broad and risk-tolerant: uncertainty does not
  delay work or publication. Only confirmed prohibition, explicit restricted
  tier, privacy exposure, or platform policy blocks.
- W0 code (H1943–H1945) is merged. H2681 completed the retrospective review:
  no open P0/P1, so the W0 review criterion passes. The one P2 Salt-profile
  extension conflict is queued as H2768 (Codex) — Resolve kosha extension on
  strict Salt compatibility faces; it does not reopen the W0 gate.

## ✅ Completed (Recent only)

- 09-08-2026 — **H2408 Heritage French glosses as an independent second reference
  for the defgen eval** (Fable 5, `claude-fable-5`): the parent protocol's own
  ranked next-step #4. The five frozen generation arms from H730/H972 were
  re-scored against Gérard Huet's French glosses on the 333-headword
  MW∩Heritage subset — **no generation re-run**, so every comparison is paired
  per item. Result: the **arm ranking is reference-invariant** (F1 > A1 > A3 >
  A2 > A0 under both the MW judge and the cross-lingual French judge), and the
  MW-familiarity premium is real but small (+0.13…+0.25 on 0–5; bootstrap CI
  excludes 0 for all four system arms, sign p ≤ 1.5e-4; largest for the
  no-context memorisation arm A2, smallest for the best arm F1). Cross-lingual
  surface metrics are near-degenerate (token-F1 0.012–0.037 vs 0.101–0.338), so
  the judged score is the measurement.
  [docs/DEFGEN_HERITAGE_SECOND_REFERENCE_EVAL.md](https://github.com/gasyoun/kosha/blob/main/docs/DEFGEN_HERITAGE_SECOND_REFERENCE_EVAL.md)
  + metadoc, harness `scripts/defgen_heritage_*.py`,
  [PR #364](https://github.com/gasyoun/kosha/pull/364), `v0.110.0`.
  **Rights mechanism worth knowing before touching this data:** Heritage
  `gloss_fr` is LGPLLR / `tier=restricted`, so no gloss text is committed — the
  subset carries SHA-256 digests and the scorer **refuses to run** if the local
  `SanskritLexicography` sibling stops reproducing them. The MW half of the
  handoff's goal line had already shipped under H730/H752/H972; only the
  Heritage half was open.
- 31-07-2026 — **H1945 W0C contract and trust boundaries shipped** (Opus 5 1M,
  `claude-opus-5[1m]`): typed Salt entry/envelope/error models and one
  serializer + one repository query layer in `src/kosha/api/`, replacing three
  hand-maintained copies; `/api/v1` and `/w/{slp1}` migrated to Salt-profile
  entries with kosha fields namespaced under `kosha` (pre-public breaking
  change, documented in
  [CONTRACT_KOSHA_API_V1_SALT_BREAKING_CHANGE.md](https://github.com/gasyoun/kosha/blob/main/docs/CONTRACT_KOSHA_API_V1_SALT_BREAKING_CHANGE.md));
  `sense[]` populated for the first time; static cards and the Salt faces now
  provably equal to the API rather than similar to it; all rendered HTML behind
  an nh3 allowlist with an adversarial suite and a golden-corpus
  non-destruction gate; Salt faces corrected to 400 (they answered 200 with an
  error body) and to explicit 400s for unindexed `query_type`s; one top-level
  error shape per contract; citation-archive path/metadata/checksum/asset-URL
  validation plus live and historical resolution smoke; the
  `archive_dir`/`KOSHA_RELEASES_DIR` configuration contradiction unified.
  Gates: **467 passed / 164 skipped** (up from 285), UI 42 passed + build
  green, fixture build from zero twice, lock audit clean. It also found that
  `main` had never been branch-protected despite the verification doc asserting
  required checks ([#223](https://github.com/gasyoun/kosha/issues/223), fixed
  the same day on human instruction); W0 now waits only on the missing Codex
  reviews — see the verdict.

- 31-07-2026 — **H1944 W0B reproducible substrate shipped** (Opus 5 1M,
  `claude-opus-5[1m]`): declarative stage DAG replacing the `if`-chain dispatch
  (closes [#210](https://github.com/gasyoun/kosha/issues/210)) with prerequisite
  checks, postconditions, temp target + atomic promotion, an immutable source
  lock, and a release mode that refuses `latest`; installable `pyproject.toml`
  + `src/kosha/` + committed dependency lock; typed settings with the
  deprecated `DATABASE_PATH` alias and hard-failing conflicts; history/auth/
  stats unmounted by default (404, absent from OpenAPI, no visitor cookie);
  committed public fixture pack building the full graph in ~2 s; required
  Python + UI CI with dependabot auto-merge gated behind both; TLS backup
  transport with temp remote name, remote-digest verification, and atomic
  rename, failing closed when the server cannot prove a digest. Gates:
  271 passed / 164 skipped (fixture tier, core DB absent in the worktree),
  UI 42 passed + build green, fixture build from zero twice with identical
  logical dumps.

- 30-07-2026 — repository, GitHub, prior-art, architecture, roadmap, quality,
  security, and verification audit completed by Codex (`gpt-5.6-sol`).
- 30-07-2026 — four-round `/ask` interview completed; 23 user rulings recorded.
- 30-07-2026 — H1943–H1945 atomically minted for Sonnet/Opus Claude Code
  execution; no Codex implementation handoff exists.
- 31-07-2026 — **H1943 W0A governance/integrity shipped** (Sonnet 5,
  `claude-sonnet-5`): fixed the two `#198` false "Gita absent from DCS"
  manifest mirrors; regenerated `directory/index.html` + README dataset
  counts (94→96) via the committed builder; established
  [docs/ROADMAP.md](https://github.com/gasyoun/kosha/blob/main/docs/ROADMAP.md)
  as the sole live roadmap; pointed root `ARCHITECTURE.md` at the platform
  contract while preserving A1–A4; bannered 4 superseded portfolio-level
  plan/roadmap docs SUPERSEDED in place; corrected the stale
  pre-alpha/no-CI/no-lookup claims in `CLAUDE.md`; added 5 fail-fast truth
  tests (`tests/test_truth_gates.py`) for version claims, active-queue
  completed markers, and required plan banners; full Python (389 passed) +
  UI (42 passed, build green) gates confirmed clean.

---

_Dr. Mārcis Gasūns_
