# Project Objective: Gasuns Sanskrit Dictionary — translator-first multi-dict lookup (codename kosha), built reuse-first over the Cologne/CDSL ecosystem, deployed as samskrtam.ru/kosha + own GitHub Pages

_Created: 02-07-2026 · Last updated: 15-07-2026_

## ➡️ Next Steps (Queue)

- **★ Kosha pedagogy engine-room build plan (H945) — new, 14-07-2026.** The sandhi
  programme is now one aspect of a full plan generalising its six-stage pattern to the
  rest of kosha's pedagogy data. Plan:
  [`PLAN_KOSHA_PEDAGOGY_ENGINE_2026_2027.md`](https://github.com/gasyoun/kosha/blob/main/PLAN_KOSHA_PEDAGOGY_ENGINE_2026_2027.md)
  (engine-room half of the org field metadoc
  [`DIGITAL_SANSKRIT_PEDAGOGY_FIELD_2026.md`](https://github.com/gasyoun/SanskritGrammar/blob/main/DIGITAL_SANSKRIT_PEDAGOGY_FIELD_2026.md), H912).
  🔴 **H946 (W1a, morphology drills) EXECUTED 15-07-2026 (Sonnet 5 `claude-sonnet-5`)** —
  see Completed below. 🔴 **H947 (W1b, vocabulary curriculum) EXECUTED 14-07-2026 (Sonnet 5
  `claude-sonnet-5`)** — see Completed below. 🔴 **H948 (W1c, samāsa trainer) EXECUTED
  14-07-2026** — [kosha PR #108](https://github.com/gasyoun/kosha/pull/108) merged. Wave 1
  is now fully closed; Wave 2+ (H949–H951) next.

- **★ DCS sandhi → pedagogy programme (H882–H900) — resume point.** Corpus-wide
  sandhi extraction for Sanskrit teaching data. Phases 0–2 shipped (v0.36→v0.42):
  junction-rule inducer (method A, **96.3% Gītā-gold frequency-mass coverage**),
  MWT vowel-coalescence + right-edge visarga, spaced-notation fix, and the merged
  [`data/sandhi/corpus_sandhi.tsv`](https://github.com/gasyoun/kosha/blob/main/data/sandhi/corpus_sandhi.tsv)
  over 8 texts (top 69 rules = 80% of all corpus sandhi). Anchor:
  [ROADMAP_CORPUS_SANDHI_PEDAGOGY_2026_2027.md](https://github.com/gasyoun/kosha/blob/main/ROADMAP_CORPUS_SANDHI_PEDAGOGY_2026_2027.md).
  Three queued directions (also in [Uprava GTD Agent-Ready](https://github.com/gasyoun/Uprava/blob/main/GTD_NEXT_ACTIONS.md)):
  - `Read C:\Users\user\Documents\GitHub\Uprava\handoffs\H901-Sonnet_kosha_sandhi-phase2b-broaden-corpus-sweep_14.07.26.md and execute it.` — broaden the sweep (Rāmāyaṇa/full MBh)
  - `Read C:\Users\user\Documents\GitHub\Uprava\handoffs\H902-Opus_kosha_sandhi-phase4-pedagogy-surfaces_14.07.26.md and execute it.` — pedagogy surfaces (needs human-confirm on the difficulty metric)
  - `Read C:\Users\user\Documents\GitHub\Uprava\handoffs\H903-Sonnet_kosha_sandhi-method-b-vidyut-cheda-bakeoff_14.07.26.md and execute it.` — Method B (Vidyut cheda bake-off)

0. **H836 (CONCORDANCE_ROADMAP Q2/B3) shipped 13-07-2026 (Sonnet 5 `claude-sonnet-5`)
   — 2 residual human items, both `@DECIDE`/`@DO` not agent-blockable.** Dataset
   `parallel-passage-concordance` (501,231 verses / 153,045 links) + web page
   [`/concordance/parallels/`](https://github.com/gasyoun/kosha/blob/main/concordance/parallels/index.html)
   live; see [CONCORDANCE_ROADMAP.md](https://github.com/gasyoun/kosha/blob/main/CONCORDANCE_ROADMAP.md)
   §Q2 for exit-check status. **Remaining:** (1) MG vote on the 2,171-item relaxed-candidate
   review sheet (`review/kosha-concordance-relaxed_q2_review.html`, gitignored,
   regenerate via `scripts/classify_relaxed_candidates.py` + `scripts/build_relaxed_review_sheet.py`
   if missing) — GTD row in Uprava; (2) MG `@DECIDE` on R-C2 (canonical PARA variant)
   and the Bloomfield digitization source (see roadmap §Q2 `@DECIDE`).

0. **kosha is now the org SANSKRIT DATA-HUB (06-07-2026, Fable 5 `claude-fable-5`, MG rulings D-HUB-1..8).**
   [DATA_HUB_ROADMAP.md](https://github.com/gasyoun/kosha/blob/main/DATA_HUB_ROADMAP.md)
   (two-tier public/restricted, samskrtam.ru canonical after deploy) +
   [data/manifest/datasets.json](https://github.com/gasyoun/kosha/blob/main/data/manifest/datasets.json)
   (15 datasets, agent contract: new/changed derived dataset => manifest row same pass) +
   first public release [data-v0.1.0](https://github.com/gasyoun/kosha/releases/tag/data-v0.1.0)
   (7 datasets, 718k rows, safety-check GO, MG-approved) — PR
   [#23](https://github.com/gasyoun/kosha/pull/23) merged, cut
   [v0.12.0](https://github.com/gasyoun/kosha/releases/tag/v0.12.0). **Next:** P-D2 backup =
   [H235](https://github.com/gasyoun/Uprava/blob/main/handoffs/archive/H235-Sonnet_kosha_data_hub_restricted_backup_06.07.26.md)
   (Sonnet 5) — **script shipped** ([scripts/deploy_guhya.py](https://github.com/gasyoun/kosha/blob/main/scripts/deploy_guhya.py),
   [PR #24](https://github.com/gasyoun/kosha/pull/24) merged), **upload itself is now MG @DO**
   (`cd kosha && python scripts/deploy_guhya.py` — harness classifier blocks
   agent-run FTP uploads of these files as exfiltration, even verify-only).
   P-D3 public NLP data+tools directory ([H236](https://github.com/gasyoun/Uprava/blob/main/handoffs/archive/H236-Opus_kosha_nlp_data_tools_directory_06.07.26.md),
   Opus 4.8 `claude-opus-4-8`) — **✅ DONE:**
   [directory/](https://gasyoun.github.io/kosha/directory/) rendered by
   [scripts/build_directory.py](https://github.com/gasyoun/kosha/blob/main/scripts/build_directory.py)
   from [datasets.json](https://github.com/gasyoun/kosha/blob/main/data/manifest/datasets.json)
   + new [external_tools.json](https://github.com/gasyoun/kosha/blob/main/data/manifest/external_tools.json)
   (9 public datasets downloadable · 6 restricted "on request" · 8 external stacks),
   schema.org Dataset JSON-LD per public asset on an Org `@id` spine, safety-check GO,
   [tests/test_directory.py](https://github.com/gasyoun/kosha/blob/main/tests/test_directory.py)
   (6 invariants), wired from README + docs-site footer, FEATURES_INDEX J21.
   P-D4 server hosting is MG deploy-gated (GTD @DO).

0. **Search history + analytics — Phases A/B/C-frontend done; 1 item remains (MG deploy-gated).**
   Personal per-visitor search history (anon cookie) + public aggregate
   analytics + the `History.svelte`/`Stats.svelte` UI (Chart.js line chart +
   top-terms table, both hidden when no live API is configured) all landed —
   see Completed. **(b) retention-purge script — ✅ DONE 09-07-2026** (Sonnet 5
   `claude-sonnet-5`, H416): [`scripts/purge_search_events.py`](https://github.com/gasyoun/kosha/blob/main/scripts/purge_search_events.py)
   + [`history_db.purge_old_search_events()`](https://github.com/gasyoun/kosha/blob/main/app/history_db.py)
   delete `search_events` rows older than `--days` (default 180); `daily_rollup`
   (the permanent anonymous per-day/per-term aggregate the `/api/v1/stats/*`
   charts read from) is never touched. `--dry-run` reports the count without
   deleting. MG-run maintenance script, no agent cron (A3). **Remaining:**
   (a) MG `@DECIDE` — pick a transactional-email provider (Resend, Postmark,
   or own SMTP) for `send_magic_link_email()` in
   [`app/history.py`](https://github.com/gasyoun/kosha/blob/main/app/history.py)
   (currently a stub that logs the link server-side) + set production
   `CORS_ORIGINS` — both MG deploy-time steps (A3), tracked in
   [Uprava/GTD_NEXT_ACTIONS.md](https://github.com/gasyoun/Uprava/blob/main/GTD_NEXT_ACTIONS.md).

0. **P3 evidence layer + P4 Wave K1 — MERGED & RELEASED.** Landed via
   [PR #9](https://github.com/gasyoun/kosha/pull/9) (branch
   `feat/p3-evidence-p4-k1-inflect`, squash of two parallel Sonnet 5
   `claude-sonnet-5` sessions that ended up file-interleaved), cut as
   [v0.7.0](https://github.com/gasyoun/kosha/releases/tag/v0.7.0). See
   Completed below for the full detail; superseded by this line the
   in-flight framing further down (branch names `p3-evidence-layer` /
   in-worktree K1 no longer exist — both are on `main`).

0. **Docs site (ZettelkastenWiki Wave-3 pilot #1) — PR #6 ожидает мёржа MG
   (03-07-2026, Fable 5 `claude-fable-5`).** `wiki/` (5 нот) →
   `scripts/build_docs_site.py` (~50 строк конфига) → коммитнутый `docs-site/`
   на существующих legacy Pages (`/kosha/docs-site/`). Тест: полный
   invariant-harness пакета + staleness-guard. Аддитивно — P2 cache tier и
   Pages-конфиг не тронуты. После мёржа проверить
   https://gasyoun.github.io/kosha/docs-site/ вживую. Правка контента: править
   `wiki/`, затем `python scripts/build_docs_site.py`, коммитить оба.


1. **P2 — public alpha (MG deploys).** Phase 1 complete (D1–D5); the P2
   static-cache **generator is now built** (branch `feat/p2-static-cache` →
   PR, release 0.5.0 — see Completed). What remains is **MG's deploy**: run the
   generator to produce the Pages tier and publish it (Pages already enabled
   03-07-2026), plus the samskrtam.ru deploy. Generate + deploy command in
   [docs/README.md](https://github.com/gasyoun/kosha/blob/main/docs/README.md);
   run `/publish-safety-check` first (A3). **P3 evidence layer is now done**
   (see Completed, release 0.7.0) — remaining sequence: P4 forms/grammar →
   P5 UI → P6 RU → P7 DOI v1.0, per
   [IMPLEMENTATION_PLAN.md](https://github.com/gasyoun/kosha/blob/main/IMPLEMENTATION_PLAN.md).
   Diplomacy triggers per [RELATIONS.md](https://github.com/gasyoun/kosha/blob/main/RELATIONS.md) §7.
2. **Search prefix/fuzzy latency** (flagged in
   [D5_MEASUREMENTS.md](https://github.com/gasyoun/kosha/blob/main/D5_MEASUREMENTS.md) §3,
   not blocking the SLO). `slp1 LIKE 'ka%'` scans the 323k-row lemmas index
   (~62–70 ms). P2/P5 fix: rewrite to a case-sensitive range seek
   (`slp1 >= 'ka' AND slp1 < 'kb'`) — also removes a latent SLP1 over-match
   (case-insensitive LIKE treats `K`=`k`, but SLP1 is case-significant).
3. **PWG multi-volume scan disambiguation** (still open, not blocking).
   `servepdf.php?page=` has no documented volume parameter; the endpoint
   returns 200 for `page=`/`vol=`/`volume=` alike, so telling whether `vol` is
   honored needs a **live content diff** against Cologne — a live third-party
   fetch (R12) + judgment, not cheap. Belongs to scan-link hardening
   (G-SCAN/R2), not D5. (D5's other flagged item — `csl_orig_commit`
   provenance — is now RESOLVED, see Completed.)
4. **P4** is specified by
   [ROADMAP_INFLECT_2026_2027.md](https://github.com/gasyoun/kosha/blob/main/ROADMAP_INFLECT_2026_2027.md)
   (MG interview 03-07-2026, rulings D1–D6): waves K1–K3 (Cologne-tables
   ingest + JSON API → modern UX + reverse lookup → dictionary integration)
   execute in the P4 slot; engine starts on Cologne tables, evolves via the
   vidyut dual-engine comparison (Wave E1). **Waves K1, K2a and K2b are now
   done** (see Completed) — nominal + verb declensions ingested, the reverse-
   lookup cascade (`inflections` → `forms` → vidyut-cheda segmentation) + the
   `stem_bridge` reconciliation behind `/api/v1/forms/{form}/analyze`, and the
   Svelte inflection UI at `gasyoun.github.io/kosha/inflect/` (H183,
   [v0.9.0](https://github.com/gasyoun/kosha/releases/tag/v0.9.0)).
   The K1 blockers are resolved: (a) the MWinflect `verbs/` Python-2 syntax
   bug is fixed + upstream-PR'd, verbs ingested; (b) the `Bagavat`/`Bagavant`
   stem reconciliation is the `stem_bridge` layer. **K3 (dictionary integration)
   was folded into K2b** (MG 05-07-2026). **Wave E1 is done end-to-end**
   (nominal comparison v0.10.0 — 90.5% agreement; hybridize forms layer +
   verb comparison v0.21.0, [H185](https://github.com/gasyoun/Uprava/blob/main/handoffs/archive/H185-Opus_kosha_e1_dual_engine_ruling_05.07.26.md)
   🔴 EXECUTED 12-07-2026). Only the **csl-inflect#10 give-back remains parked**
   (diplomacy-gated, awaits a separate MG post go-ahead).
   Independent upstream side track:
   [H093](https://github.com/gasyoun/Uprava/blob/main/handoffs/H093_cslinflect_upstream_probe.md)
   (probe PR) +
   [H094](https://github.com/gasyoun/Uprava/blob/main/handoffs/H094_cslinflect_question_research.md)
   (issue research give-back) — runnable any time, not gated on the queue.

## 🚧 Current Work-In-Progress (WIP)

**H955 — last-mile pipeline Rung B1 demo, kosha side DONE 15-07-2026 (Sonnet 5 `claude-sonnet-5`).**
🔴 EXECUTED (kosha half): [PR #111](https://github.com/gasyoun/kosha/pull/111) merged.
[`scripts/build_demo_srs_deck.py`](https://github.com/gasyoun/kosha/blob/main/scripts/build_demo_srs_deck.py)
emits `data/srs/srs-deck-b1-demo.json` (manifest id `kosha-srs-deck-b1-demo`) — content
vocabulary of the existing `dcs-reading-pack-nala-1`, `core_rank`-ordered, function words
stripped (wave-1a method); 439 tokens → 164 cards, full accounting in the file's `stats`
block. Deliberately scoped to ONE reading pack, not H947's general `vocab_curriculum.tsv`
(checked for overlap before starting — no duplication). Proves
[`docs/LAST_MILE_PIPELINE_SPEC.md`](https://github.com/gasyoun/SanskritGrammar/blob/main/docs/LAST_MILE_PIPELINE_SPEC.md)
Hop B end-to-end; Systema-side import landed same day
([Systema-Sanscriticum PR #519](https://github.com/gasyoun/Systema-Sanscriticum/pull/519),
`srs:import-kosha-b1-demo`, flag-gated `features.kosha_srs` OFF by default). Handoff:
[H955](https://github.com/gasyoun/Uprava/blob/main/handoffs/H955-Sonnet_kosha-Systema-Sanscriticum_last-mile-pipeline-rung-b1-demo_15.07.26.md).
**Next (not this handoff):** Hop A (reader route) and Hop C (difficulty-score consumption)
are still open per the spec — H949 (graded-reader + difficulty scorer) covers the kosha-side
half; no Systema-side reader route exists yet.

**P5 advanced-UI build DONE — steps 1–6a (11-07-2026, Opus 4.8 `claude-opus-4-8`, H537).**
🔴 EXECUTED: H537-Opus_kosha_p5_advanced_ui_word_page_build_10.07.26.md. Built from
[P5_ADVANCED_UI_DESIGN.md](https://github.com/gasyoun/kosha/blob/main/P5_ADVANCED_UI_DESIGN.md)
(design session was already done): the crawlable word page over the whole P1–P4 stack.
- **Shared template** [app/word_page.py](https://github.com/gasyoun/kosha/blob/main/app/word_page.py) —
  every dict panel in the DOM (active shown, rest hidden) + `<noscript>` stacked fallback
  (§5 crawlability), Gloss/Full/Adaptive view modes (P5-2), host-independent links
  (R1/R5), correct `slp1_to_devanagari` (not the broken `iast_to_devanagari` path).
- **Static prerender** [scripts/build_word_pages.py](https://github.com/gasyoun/kosha/blob/main/scripts/build_word_pages.py) —
  runs off the committed card set (no DB) → `/w/<token>.html` + `/browse/<varṇa>` spine;
  logs N + Pages budget + dropped tail. **Budget finding:** word pages ≈ **9.7 KB/page on
  the head**; full N=50,355 ≈ **~478 MB**, which on top of the 402 MB card set exceeds the
  ~1 GB Pages soft cap — MG should ship the static head (top-N) + let the SSR tail cover
  the rest (the design already routes it this way). `docs/w/` + `docs/browse/` gitignored
  (regenerable, like cards).
- **SSR** `GET /w/{slp1}` in [app/main.py](https://github.com/gasyoun/kosha/blob/main/app/main.py) —
  same template → byte-parity with the prerender ([tests/test_word_page.py](https://github.com/gasyoun/kosha/blob/main/tests/test_word_page.py),
  15 no-DB + 1 DB-gated SSR parity check).
- **SPA** `WordPage.svelte` + `#/w/{slp1}` hash routing, **operators** `root:`/`sandhi:`
  ([lib/query.js](https://github.com/gasyoun/kosha/blob/main/ui/src/lib/query.js)), and
  **CSV/Anki export** ([lib/export.js](https://github.com/gasyoun/kosha/blob/main/ui/src/lib/export.js));
  42 vitest green, word-page + `sandhi:` flows verified in-browser.

**Remaining P5:**
- **Step 6b — reading packs: Nala 1 DONE (H848, Sonnet 5 `claude-sonnet-5`); Gītā 1 parked.**
  The "DCS absent on disk" note was WRONG — the real 920 MB DB is
  `VisualDCS/src/DCS-data-2026/dcs_full.sqlite` (the 0-byte `src/dcs_full.sqlite` is a decoy).
  **Nala 1** (`MBh, 3, 50`) built:
  [scripts/build_reading_pack.py](https://github.com/gasyoun/kosha/blob/main/scripts/build_reading_pack.py)
  reuses the H380 concordance core (`TieredMatcher`/`to_slp1`/`card_token`) →
  [reading/](https://github.com/gasyoun/kosha/blob/main/reading/index.html) static viewer,
  65 sentences / 439 tokens / **434 (98.9%) linked to `/w/` cards** (exact tier; the 5
  unlinked = DCS causative/denominative `-ay` stems + 1 indeclinable, honest residue in
  [reading/BUILD_REPORT.md](https://github.com/gasyoun/kosha/blob/main/reading/BUILD_REPORT.md)).
  Dataset `dcs-reading-pack-nala-1` in `datasets.json`. **Gītā built GOLD — all 18 adhyāyas**
  (H848 Task B + roadmap W1/H871): the Bhagavadgītā is absent from DCS, so the word-by-word
  analysis comes from the hand-curated **`SanskritGrammar/Concordance/Gita.xlsm`** (MG named it
  13-07-2026), vendored as the W0 master
  [data/gita/gita_gold_master.tsv](https://github.com/gasyoun/kosha/blob/main/data/gita/gita_gold_master.tsv)
  (9,092 words · 18 adhyāyas). [scripts/build_reading_pack_gita.py](https://github.com/gasyoun/kosha/blob/main/scripts/build_reading_pack_gita.py)
  builds **all 18 chapter packs** (`reading/data/gita-1..18.js`) off the master — **701 verses /
  9,092 words / ~99.5% linked** to `/w/` cards, Devanagari + IAST + English glosses, chapter picker
  in [reading/index.html](https://github.com/gasyoun/kosha/blob/main/reading/index.html); dataset
  `gita-reading-pack`. The chapter-1-only source + `extract_gita_gold.py` (and the earlier
  experimental GRETIL/vidyut-cheda build) are retired. Same gold class as Nala. Downstream
  Gītā-gold workstreams: W3 morphology (H873 ✅) · W4 inflection QA (H874 ✅) · W2/W5/W6 queued.
- **Step 7 — exit checks: MG-gated.** MG sign-off on live staging · Lighthouse mobile ≥90
  · paste-a-Gītā-verse walkthrough — all wait on MG's P2 `samskrtam.ru` deploy (GTD @DO).

**E1 verb dhātu-identity crosswalk DONE (13-07-2026, Opus 4.8 `claude-opus-4-8`, H855).**
🔴 EXECUTED: H855-Opus_kosha_e1-verb-dhatu-identity-crosswalk_13.07.26.md.
- Closed the H185 Task C 12.68 % verb-agreement mapping artifact. New
  [scripts/build_dhatu_crosswalk.py](https://github.com/gasyoun/kosha/blob/main/scripts/build_dhatu_crosswalk.py)
  matches Cologne bare roots → vidyut aupadeśika dhātus (present-3sg / direct /
  normalized-bare), resolving **722/779 (92.7 %)** gaṇa-1/4/6/10 root-models;
  committed `data/e1/dhatu_crosswalk.json` (aupadeśika strings only → the
  comparison needs only bundled vidyut; `vidyut-data` is a build-time input, R12).
  `compare_vidyut_verbs.py` re-run: strict agreement **12.68 % → 70.24 %**,
  roots-vidyut-can't-derive 259 → 110, `COLOGNE_ONLY` 29,268 → 15,984. Report
  Verbs section rewritten + ṇatva/gap-fill/disputed counts reconciled to the
  shipped DB; `tests/test_dhatu_crosswalk.py` (6, green). No verb hybridization
  (D3); Task B give-back stays MG-gated.

**E1 hybridize + verb comparison DONE (12-07-2026, Opus 4.8 `claude-opus-4-8`, H185,
[v0.21.0](https://github.com/gasyoun/kosha/releases/tag/v0.21.0)).**
🔴 EXECUTED: H185-Opus_kosha_e1_dual_engine_ruling_05.07.26.md.
- **Task A (hybridize forms layer).**
  [scripts/build_hybrid_forms.py](https://github.com/gasyoun/kosha/blob/main/scripts/build_hybrid_forms.py)
  (`build_db.py --stage hybrid`) layers vidyut over the Cologne `inflections` base,
  reusing the E1 classifier so the applied set matches
  [E1_DIVERGENCE_REPORT.md](https://github.com/gasyoun/kosha/blob/main/E1_DIVERGENCE_REPORT.md)
  cell-for-cell (top-10k): **326 ṇatva cells / 89 stems** → `hybrid-natva-fix` rows;
  **16 `m_card` cells** → `vidyut-gap-fill`; **13,770 cells / 1,440 stems** → `disputed=1`
  (13,666 `other` + 104 `pronominal`); final_stop (7,454) + supersets left as-is.
  No Cologne row deleted (buggy form stays reverse-resolvable);
  [app/paradigm.py](https://github.com/gasyoun/kosha/blob/main/app/paradigm.py) prefers
  the fix + emits sparse per-model `cell_notes`,
  [app/reverse_lookup.py](https://github.com/gasyoun/kosha/blob/main/app/reverse_lookup.py)
  adds `source`/`disputed` per parse. New `disputed` column (schema + migration).
  Full 235,849-paradigm run is MG's out-of-band `--stage hybrid` (A3), like
  `build_paradigms --all`.
- **Task C (verb comparison, answers #8).**
  [scripts/compare_vidyut_verbs.py](https://github.com/gasyoun/kosha/blob/main/scripts/compare_vidyut_verbs.py)
  vs vidyut `Tinanta`: strict agreement only **12.68 %** (683 roots) — a dhātu-IDENTITY
  mapping gap (bare Cologne root ≠ vidyut aupadeśika), not a grammar disagreement; **no
  verb hybridization applied** (would inject a different lexeme). Dhātu-identity crosswalk
  = the flagged larger follow-on.
- **Task B (csl-inflect #10 give-back): still PARKED** — diplomacy-gated, awaits a separate
  MG go-ahead (RELATIONS.md §2/§7). Draft unchanged, NOT posted.
- 6 new tests ([tests/test_hybrid_forms.py](https://github.com/gasyoun/kosha/blob/main/tests/test_hybrid_forms.py)),
  demo shards regenerated; **229 passed / 2 skipped**.

Nothing else in flight. **H380 Q1 concordance DONE** (10-07-2026, Fable 5 `claude-fable-5`):
the concordance program's shared core ([scripts/concordance_core.py](https://github.com/gasyoun/kosha/blob/main/scripts/concordance_core.py) —
schema + tiered matcher on canonical sanskrit-util keys + host-independent `dcs:<sent_id>`
loci, imported by Q2–Q4) + the B1 dataset ([data/concordance/](https://github.com/gasyoun/kosha/tree/main/data/concordance):
74,520 asserted links, xref 12,836 / exact 61,373 / floor 311; coverage sidecar classes all
323,425 headwords, 66,257 attested = 20.5%) + manifest row + the reusable static viewer
([concordance/dict/](https://github.com/gasyoun/kosha/tree/main/concordance/dict), 25 shards).
Golden sample: mechanical 14/14; the lossy `norm` tier was 0/3 semantically right →
**2,171 relaxed links quarantined** to `dict_corpus_relaxed_candidates.tsv` (review queue for
Q2, never asserted). Q2 (B3 parallel-passage) is the next concordance handoff to mint at
its kickoff per the roadmap. Earlier: **H345 MW↔Heritage crosswalk ingest DONE** (08-07-2026, Fable 5
`claude-fable-5`/Sonnet 5 `claude-sonnet-5` continuation): `heritage_anchor` table +
`--stage heritage` in [scripts/build_db.py](https://github.com/gasyoun/kosha/blob/main/scripts/build_db.py)
ingests [mw_heritage_crosswalk.tsv](https://github.com/gasyoun/SanskritLexicography/blob/master/HeadwordLists/mw_heritage_crosswalk.tsv)
verbatim (185,803 MW keys, 25,140 Heritage-covered / 24,549 anchor-resolved / 591 unresolved);
`heritage` witness surfaced in [app/main.py](https://github.com/gasyoun/kosha/blob/main/app/main.py)
`_entry_payload` (mirrored in [scripts/build_static_cache.py](https://github.com/gasyoun/kosha/blob/main/scripts/build_static_cache.py)'s
`entry_payload` — caught by `test_static_cache.py`'s card==live-API contract test, which failed
until mirrored) + `/api/v1/meta` count; `HERITAGE_DICO_BASE` in `.env.example`; fixed a worktree
sibling-path bug (`SIBLING = ROOT.parent` resolved to `.claude/worktrees/` instead of `GitHub/`
when run from inside a git worktree — now walks up to the dir containing `SanskritLexicography/`);
`data/manifest/datasets.json` `mw-heritage-crosswalk` consumers flipped from queued to done. All
183 tests pass. **P4 Wave K2 fully done** (K2a H181 + K2b H183,
[v0.9.0](https://github.com/gasyoun/kosha/releases/tag/v0.9.0) — live UI at
`gasyoun.github.io/kosha/inflect/`, K3 folded in). **Wave E1 nominal comparison
also done** (05-07-2026, Opus 4.8 `claude-opus-4-8`,
[v0.10.0](https://github.com/gasyoun/kosha/releases/tag/v0.10.0)): vidyut-prakriya
vs Cologne, **90.5% cell agreement** over 240k cells, ṇatva blast-radius > the 69
([E1_DIVERGENCE_REPORT.md](https://github.com/gasyoun/kosha/blob/main/E1_DIVERGENCE_REPORT.md)).
**E1 remainder DONE 12-07-2026** (Opus 4.8 `claude-opus-4-8`,
[v0.21.0](https://github.com/gasyoun/kosha/releases/tag/v0.21.0)): 🔴 EXECUTED
[H185](https://github.com/gasyoun/Uprava/blob/main/handoffs/archive/H185-Opus_kosha_e1_dual_engine_ruling_05.07.26.md)
— (a) hybridize @DECIDE ruled + forms-layer implemented (Task A), (c) verb comparison run
(Task C, dhātu-identity gap); (b) the **csl-inflect#10 give-back stays parked**
(diplomacy-gated, still awaits a separate MG post go-ahead). See the WIP block above.

MG deploys the full `build_paradigms.py --all` paradigm set + the full `--stage hybrid`
pass out-of-band (A3).

## 🧠 Dev Notes & Hypotheses (Bugs, ideas, context)

- **Wave K1/E1 (forms sidecar) — csl-inflect caveats from the H094 research sweep
  (03-07-2026, Fable 5 `claude-fable-5`):** (1) csl-inflect's m_a declensions carry a
  live ṇatva bug — the Pāṇini 8.4.12 dehyphenation agreed in
  [MWinflect#6](https://github.com/sanskrit-lexicon/MWinflect/issues/6) (69 compounds:
  nṛpa, turaga, uraga…) was never implemented, so it emits `nfpena`/`nfpAnAm` for
  attested `nfpeRa`/`nfpARAm` — do NOT ingest those cells uncorrected (details:
  [csl-inflect#13 comment](https://github.com/sanskrit-lexicon/csl-inflect/issues/13#issuecomment-4872549254)).
  (2) Perfect/verb tables there are extracted from Huet's SL_morph.xml of June 2020,
  but upstream `sanskrit.inria.fr/DATA/XML/SL_morph.xml.gz` was regenerated 20-06-2026 —
  for perfect-form coverage, extract fresh (via the UoHyd mirror per the note above)
  rather than reusing csl-inflect's 2020 snapshot. (3) j-stem corrections live in
  [corrections_j.txt](https://github.com/sanskrit-lexicon/csl-inflect/blob/master/nominals/pysanskritv2/tables/corrections/corrections_j.txt)
  (only 4 active overrides; ~30 candidates still commented, e.g. yuj dual strong forms).

- **All product/engineering decisions are locked and written down** — do not
  re-litigate: M1–M4 (triage meta-decisions,
  [KOSHA_FOLDER_SETUP.md](https://github.com/gasyoun/SanskritLexicography/blob/master/KOSHA_FOLDER_SETUP.md)),
  A1–A4 + Salt max-reuse rules
  ([ARCHITECTURE.md](https://github.com/gasyoun/kosha/blob/main/ARCHITECTURE.md)),
  licenses (code CC BY-NC 4.0 / data CC BY-SA 4.0 —
  [LICENSE.md](https://github.com/gasyoun/kosha/blob/main/LICENSE.md),
  [LICENSE-DATA.md](https://github.com/gasyoun/kosha/blob/main/LICENSE-DATA.md);
  NB Cologne is BY-**SA** — NC cannot attach to derived data), UI override
  (own advanced UI, API-first —
  [POSITIONING.md](https://github.com/gasyoun/kosha/blob/main/POSITIONING.md) §2).
- **Entry data source = csl-sqlite releases** (same Dal layer as csl-apidev's
  run-verified Salt implementation); csl-orig text parse is fallback only.
- **Trust only the dated live survey** for competitor claims
  ([COMPARISON.md](https://github.com/gasyoun/kosha/blob/main/COMPARISON.md),
  02-07-2026; mirrored as
  [FINDINGS §41](https://github.com/gasyoun/SanskritLexicography/blob/master/FINDINGS.md)) —
  the pre-triage docs in SanskritLexicography carried fabricated numbers and
  are banner-flagged.
- Heritage integration must use the UoHyd mirror (Inria host is bot-walled).
- **Changelog discipline:** [CHANGELOG.md](https://github.com/gasyoun/kosha/blob/main/CHANGELOG.md)
  is SemVer, Keep-a-Changelog; every changelog edit promotes [Unreleased] →
  next `x.y.z` + tag + GitHub release in the same pass. Repo `vX.Y.Z` ≠
  `data_version` (sense citations pin to the latter).
- [USE_CASES.md](https://github.com/gasyoun/kosha/blob/main/USE_CASES.md)
  (UC1–UC13) acceptance seeds are now real named tests T-UC1…T-UC13 in
  [EVAL_PLAN.md](https://github.com/gasyoun/kosha/blob/main/EVAL_PLAN.md) §6
  (Gītā 1.1 locked as the UC6 verse; negative tests are the primary gate for
  UC4/UC5).
- **RISKS.md commitments that change earlier plans:** Zenodo mirroring of
  data releases starts at the **first citable release**, not P7 (R1c); the
  D4 `cite` object must resolve old versions in a browser (R1a, forced by
  T-UC10); builds never call live third-party services (R12).
- **Stale-clone caveat:** the local `SanskritLexicography` clone does not yet
  contain FINDINGS §41 (the COMPARISON mirror, PR
  [#55](https://github.com/gasyoun/SanskritLexicography/pull/55)) — pull
  before citing §41 locally; content is identical to
  [COMPARISON.md](https://github.com/gasyoun/kosha/blob/main/COMPARISON.md).

## ✅ Completed

- **H946 W1a — morphology drills (15-07-2026, Sonnet 5 `claude-sonnet-5`).** 🔴 EXECUTED:
  H946-Sonnet_kosha_pedagogy-w1-morphology-drills_14.07.26.md. Turned the P4 paradigm
  engine ([`app/paradigm.py`](https://github.com/gasyoun/kosha/blob/main/app/paradigm.py))
  into graded, frequency-filtered, answer-keyed drills — the novel move: drill only forms
  the corpus actually attests. New one-pass VisualDCS `dcs_full.sqlite` attestation join
  in [`scripts/build_morphology_drills.py`](https://github.com/gasyoun/kosha/blob/main/scripts/build_morphology_drills.py):
  `(lemma, form, morphology)` → corpus locus/count, DCS UD case/gender/number tags mapped
  directly onto Cologne's vocabulary, verb person/number/tense-mood collapsed onto kosha's
  `pre`/`ipf`/`ipv`/`opt` axis. **Real data gap found and handled honestly, not forced:**
  DCS's `m_unsandhiedreconstructed` (clean citation form) is null for 100% of VERB tokens
  and ~29% of nominal tokens in this snapshot — fell back to the raw in-context `form`
  field for those (a literal-string match, so word-boundary sandhi can only cause a real
  attested form to be MISSED, never falsely claimed); DCS's `feat_voice` marks only
  passive, so verb voice (active/middle) is not part of the match key — documented
  limitation, not a silent overclaim. Over the 5,985 core-vocabulary lemmas with entries:
  38,782/254,805 candidate cells (15.2%) survived attestation.
  [`data/morphology/morphology_curriculum.tsv`](https://github.com/gasyoun/kosha/blob/main/data/morphology/morphology_curriculum.tsv)
  (7,134 paradigms, class-bucket graded: a-stems → other-vowel-stems → consonant-stems →
  pronouns → present-class-verbs → other, per
  [`drill_weights.json`](https://github.com/gasyoun/kosha/blob/main/data/morphology/drill_weights.json),
  tunable). **Coverage headline:** learn 4,862 paradigms → cover 50% of attested
  nominal/verbal tokens, 6,351 → 80%, 6,708 → 90% (bucket-first ordering trades
  coverage-efficiency for pedagogical sequencing — expected, not a defect).
  [`data/morphology/drills.json`](https://github.com/gasyoun/kosha/blob/main/data/morphology/drills.json)
  (12,000 fill/match items, scoped to the top 6,000/38,782 attested cells by frequency —
  `--max-drill-cells 0` rebuilds the full set, same discipline as `build_sandhi_drills.py`'s
  `--max-lesson`; unscoped inline JSON would have made the HTML page ~41 MB) +
  `morphology_drills.apkg` (Anki, 6,000 notes, verified re-imports as a valid collection) +
  `reading/morphology/{curriculum,drills}/index.html` (self-contained, theme-aware,
  Devanāgarī/IAST/SLP1 toggle on the curriculum page, fill/match type filter on the
  drills quiz — both verified live in-browser, no console errors).
  `morphology-curriculum` + `morphology-drills` manifest rows.
  [`tests/test_morphology_drills.py`](https://github.com/gasyoun/kosha/blob/main/tests/test_morphology_drills.py)
  (11 tests, all green: every item answer-keyed and evidence-backed, no unattested form in
  default mode, coverage monotone, lesson-bucket ordering never regresses, Anki note count
  matches). Full `pytest` run: 280/281 relevant tests pass; the 1 failure
  (`test_no_restricted_download_leak`, `nala-1.json`) is pre-existing and unrelated,
  confirmed present on a clean `origin/main` checkout too. Built in an isolated worktree
  (`kosha-h946-morphology-drills`, kosha is shared-tree-guarded); `data/db/kosha.db`
  copied in (gitignored, never present in a fresh worktree, per H947/H950 precedent).
  `CHANGELOG.md` → v0.54.0 + `CITATION.cff` bumped.

- **H947 W1b — frequency-graded vocabulary curriculum (14-07-2026, Sonnet 5
  `claude-sonnet-5`).** 🔴 EXECUTED:
  H947-Sonnet_kosha_pedagogy-w1-vocabulary-frequency-curriculum_14.07.26.md.
  Applied the sandhi-curriculum method to words over the Leonchenko
  core-vocabulary layer in
  [lemma_frequency.tsv](https://github.com/gasyoun/kosha/blob/main/data/frequency/lemma_frequency.tsv)
  (`core_rank`/`coverage_pct`). New
  [scripts/build_vocab_curriculum.py](https://github.com/gasyoun/kosha/blob/main/scripts/build_vocab_curriculum.py) →
  [data/frequency/vocab_curriculum.tsv](https://github.com/gasyoun/kosha/blob/main/data/frequency/vocab_curriculum.tsv)
  (6,667 lemmas / 134 lessons of 50; 453 core lemmas dropped for having no
  committed dictionary card — no dead `/w/` links), `vocab_drills.json`
  (13,334 recognition/recall items) + `vocab_curriculum.apkg` (Anki, verified
  re-imports as a valid collection) +
  [reading/vocabulary/curriculum/index.html](https://github.com/gasyoun/kosha/blob/main/reading/vocabulary/curriculum/index.html).
  **Coverage headline:** 284 lemmas → 30%, 1,122 → 50%, 4,978 → 70% of the
  core-vocabulary corpus mass. **Gotcha caught before building:**
  `data/frequency/README.md` explicitly warns `coverage_pct` is a per-lemma
  MARGINAL weight, not already-cumulative — the cumulative column is computed
  by this script (running sum in `core_rank` order), not copied from the
  source feed; a naive copy would have silently violated the "monotone
  coverage" acceptance test. **Gloss source decision:** tried DCS's
  `lemma.meanings` first (cleaner short glosses) but it only exact-matches
  ~23% of this lemma set against `dcs_full.sqlite` — rejected as primary;
  used the lemma's own committed `docs/cards/<token>.json` (MW preferred via
  `DICT_ORDER`) instead, which covers 100% of kept rows by construction (a
  card-less lemma is dropped, never included with an empty gloss).
  `tests/test_vocab_curriculum.py` (8 tests, all green: monotone coverage,
  every lemma resolves to a real card, lesson sizes, drill-item
  answer/evidence completeness, apkg written). `datasets.json` gains
  `vocab-curriculum` + `vocab-drills` rows. Full `pytest` run in the build
  worktree shows 92 pre-existing failures, all `sqlite3.OperationalError:
  unable to open database file` — the worktree simply lacks the gitignored,
  locally-built `kosha.db` (never copied into fresh worktrees), unrelated to
  this change; this file's own tests need no DB. Built in an isolated
  worktree (`kosha-h947-vocab`, kosha is shared-tree-guarded).

- **H950 (pedagogy Wave 2, W2b): roots frequency + attestation curriculum
  (14-07-2026, Sonnet 5 `claude-sonnet-5`).** REUSE/INTEGRATE, not a rebuild —
  found [WhitneyRoots](https://github.com/gasyoun/WhitneyRoots) already
  computes a per-root MW↔Whitney↔DCS triangulation with corpus frequency and
  attested forms (`WhitneyRoots/src/dcs_freq.json`, generated 2026-06-10) via
  its canonical `scripts/root_triangulation.py` — the prior-art re-check the
  handoff itself asked for. New
  [`scripts/build_roots_frequency.py`](https://github.com/gasyoun/kosha/blob/main/scripts/build_roots_frequency.py)
  reads that source and adds the missing graded-curriculum framing (rank +
  cumulative `coverage_pct`), producing
  [`data/roots/roots_frequency.tsv`](https://github.com/gasyoun/kosha/blob/main/data/roots/roots_frequency.tsv)
  + `.json` (629 unique-DCS-lemma rows, deduped from 717 attested Whitney-hub
  roots — **caught and fixed a real bug pre-commit**: 74 homonym-shared
  lemmas each report the *same* corpus total on every homonym sharing them;
  summing naively would have triple-counted that mass and produced a
  nonsensical >100%-looking coverage curve). Coverage headline: top 25 roots
  → 58.7%, top 50 → 71.7%, top 100 → 85.4%, top 200 → 95.3% of verb-token
  occurrences (549,549 total). `roots-frequency-curriculum` manifest row
  registered. No kosha roots UI shipped, per the architecture's
  integration-surface rule. `tests/test_roots_frequency.py` (8/8 green,
  isolated). Full `pytest` run showed 92 pre-existing failures, all
  `sqlite3.OperationalError: unable to open database file` — `data/db/kosha.db`
  is gitignored and absent in the fresh worktree used for this build
  (regenerable, hours of compute); unrelated to this change, confirmed by
  tracing one failure to `app/db.py`'s `get_db()`. `CHANGELOG.md` → v0.50.0 +
  `CITATION.cff` bumped.

- **H901 sandhi Phase 2b — broadened corpus sweep, 8→17 texts (14-07-2026,
  Sonnet 5 `claude-sonnet-5`).** Extended
  [scripts/build_corpus_sandhi.py](https://github.com/gasyoun/kosha/blob/main/scripts/build_corpus_sandhi.py)'s
  `TEXTS` list: + kāvya (Buddhacarita, Kumārasaṃbhava, Kirātārjunīya,
  Meghadūta), + readers (Daśakumāracarita, Bhāratamañjarī), + śataka/nīti
  (Śatakatraya, Bhallaṭaśataka), + full Rāmāyaṇa (606 files) + full
  Mahābhārata (1,995 files, replacing H900's Bhagavadgītā-only glob — the
  Gītā's chapters are already inside the full sweep, so the narrow row was
  retired rather than double-counted). **580,230 events · 9,840 rules; top 83
  rules cover 80%** (was 53,291 / 1,674 / top 69). **Scale finding:** the
  handoff worried the full Mahābhārata would need a parvan/`--limit` sample
  gate — measured instead of assumed (~10 files/s), the full corpus ran
  directly in a few minutes, no gate needed. Manifest row + roadmap +
  `.meta.md` updated. Full test suite green (242/242 relevant; 1 pre-existing
  unrelated failure, `nala-1.json`, confirmed present on clean HEAD too).

- **H896 Bloomfield RV pratīka cross-reference (13-07-2026, Sonnet 5 `claude-sonnet-5`) —
  resolves H836's Bloomfield-source `@DECIDE`.** MG obtained direct written permission from
  Marco Franceschini (University of Bologna) for his digital edition of Bloomfield's 1906
  *A Vedic Concordance* (Harvard Oriental Series 9) — non-exclusive/worldwide/perpetual, public
  tier, citation-only requirement (grant text:
  [data/manifest/rights/franceschini_hos9_permission_2026-07-13.md](https://github.com/gasyoun/kosha/blob/main/data/manifest/rights/franceschini_hos9_permission_2026-07-13.md)).
  Source found already present locally at
  [SanskritLexicography/review/HOS9-Bloomfield-VedConcord1906/](https://github.com/gasyoun/SanskritLexicography/tree/master/review/HOS9-Bloomfield-VedConcord1906)
  (UTF-16, not plain ASCII despite the `.txt` extension). New
  [scripts/build_bloomfield_rv_crossref.py](https://github.com/gasyoun/kosha/blob/main/scripts/build_bloomfield_rv_crossref.py)
  extracts 36,680 direct RV citations (10,374 distinct maṇḍala-sūkta-verse keys) from the
  88,835-entry concordance, adds a `bloomfield_pratika` column to
  `parallel_passage_verses.tsv` for the RV subset. **Key finding (not assumed):**
  `parallel_passage_verses.tsv` splits every RV verse into exactly 2 printed half-lines, which
  does NOT line up 1:1 with Bloomfield's grammatical a/b/c/d pada lettering for 4-pada meters —
  a naive positional row→letter mapping would have silently mismatched. Fixed by bucketing a/b
  to half 1, c/d to half 2, then independently text-validating every candidate (parenthetical
  variant-reading asides stripped, `form_key()`-normalized, pratīka's own final character
  dropped to tolerate Bloomfield's pausa-form citation vs the half-line's actual external
  sandhi at that boundary) — raised the validated rate from a naive first pass (57%) to **85%**
  (11,522/13,581); the residual 15% is genuine orthographic variance between the two
  independently-produced digitizations (anusvāra-vs-homorganic-nasal spelling,
  consonant-doubling), documented not chased further
  ([BLOOMFIELD_RV_CROSSREF_REPORT.md](https://github.com/gasyoun/kosha/blob/main/data/concordance/BLOOMFIELD_RV_CROSSREF_REPORT.md)).
  New standalone dataset `bloomfield-rv-citations` (36,680 rows) + manifest rows + `LICENSE-DATA.md`
  entry + roadmap `@DECIDE` flipped + `/concordance/parallels/` viewer updated to surface the
  pratīka when present. R-C2 (canonical PARA variant) remains the one open `@DECIDE`.

- **H836 concordance Q2/B3 parallel-passage build + relaxed-candidate review sheet
  (13-07-2026, Sonnet 5 `claude-sonnet-5`).** Dataset `parallel-passage-concordance`
  (manifest row) from `VisualDCS` PARA/Polnorazmernye/ (245 CSVs; 501,231 verses,
  15,164 with ≥1 parallel, 153,045 GOOD+PARTLY links — supersedes the roadmap's
  prior unverified "506,787" estimate) + web viewer `concordance/parallels/` +
  directory page regenerated. Task A: 2,171 Q1 relaxed candidates pre-classified
  (740 worth-a-closer-look / 1,431 likely-spurious) and a Russian-language
  `/review-sheet` emitted for MG (gitignored, GTD-registered). Two `@DECIDE`s
  surfaced, not self-ruled: R-C2 canonical PARA variant; Bloomfield digitization
  source for the RV cross-reference (not found anywhere in the org — gap
  documented, not fabricated).

- **H752 parked-lane salvage (11-07-2026, Fable 5 `claude-fable-5`).**
  🔴 EXECUTED: H752-Fable_kosha_defgen-parked-lane-salvage_11.07.26.md. Grafted into the
  merged H730 protocol ([docs/DEFGEN_MW_GLOSS_EVAL_PROTOCOL.md](https://github.com/gasyoun/kosha/blob/main/docs/DEFGEN_MW_GLOSS_EVAL_PROTOCOL.md)):
  verified Hellwig-2026 delta (supervised MW-definition WSD, Sanskrit Sembank gold —
  overlap check closed), SHA-256 input digests in
  [frozen_sample.meta.json](https://github.com/gasyoun/kosha/blob/main/data/eval/defgen/frozen_sample.meta.json)
  (match the parked lane's independent digests byte-for-byte — both racing lanes consumed
  identical inputs), gold-free inputs projection + 3-rater Fleiss-κ WSD design queued as
  protocol next-steps #7 for the A59 paper. **Canonical-sample ruling in writing:** the
  500-headword seed-730 sample is the single frozen set of record; the parked
  520-headword rival was never scored; branch `h730-defgen-eval-fable-lane` deleted
  after salvage ([Uprava FINDINGS §67](https://github.com/gasyoun/Uprava/blob/main/FINDINGS.md)).

- **H730 definition-generation + gloss-grounded WSD eval (11-07-2026, Fable 5 `claude-fable-5`).**
  First CDSL-side definition-modeling eval on Sanskrit: frozen 500-headword MW sample
  (3×3 freq×polysemy strata, seed 730, [data/eval/defgen/](https://github.com/gasyoun/kosha/tree/main/data/eval/defgen)),
  4 arms (random floor, deepseek-chat ±attestations, deepseek-reasoner), sacrebleu
  BLEU/chrF + token-F1 + gated blinded LLM judge, WSD inter-model agreement pilot
  (50 poly5p lemmas — agreement-only, DCS `m_wordsem` inventory not locally decodable).
  Protocol + results: [docs/DEFGEN_MW_GLOSS_EVAL_PROTOCOL.md](https://github.com/gasyoun/kosha/blob/main/docs/DEFGEN_MW_GLOSS_EVAL_PROTOCOL.md);
  manifest row `mw-defgen-eval-sample`. Headline: chrF/token-F1 separate systems from
  floor cleanly; corpus context adds little over parametric MW memorization (A1≈A2) —
  contamination framing is load-bearing for the paper (eLex 2027 / EURALEX 2028 / IJL).

- **11-07-2026 (Fable 5 `claude-fable-5`) — H696: Heritage surplus forms
  default-off per the R7 ruling** ([PR #57](https://github.com/gasyoun/kosha/pull/57)
  merged, [v0.19.0](https://github.com/gasyoun/kosha/releases/tag/v0.19.0)).
  The R7 ruling (10-07-2026, [Uprava docs/DECISIONS_roadmap_forks_2026H2.md](https://github.com/gasyoun/Uprava/blob/main/docs/DECISIONS_roadmap_forks_2026H2.md))
  "ingest, provenance-flagged, default-off": ingest + provenance were already
  H111's (951,991 `forms.source='heritage'` rows; re-verified **928,262
  distinct forms = exactly the ruled ~928k**; the feed has no case/number
  parses, so `forms` — not `inflections` — is the correct layer; `inflections`
  stays pure `cologne_mwinflect`). H696 delivered the never-enforced
  default-off half: `?heritage=1` opt-in on
  [`/api/v1/form`](https://github.com/gasyoun/kosha/blob/main/app/main.py) and
  [`/api/v1/forms/{form}/analyze`](https://github.com/gasyoun/kosha/blob/main/app/reverse_lookup.py)
  (stages 2–3 incl. per-pada segmentation re-resolution); static tier
  ([`scripts/build_paradigms.py`](https://github.com/gasyoun/kosha/blob/main/scripts/build_paradigms.py))
  excludes heritage outright + committed demo reverse shards regenerated.
  [`tests/test_heritage_default_off.py`](https://github.com/gasyoun/kosha/blob/main/tests/test_heritage_default_off.py)
  (8 tests; suite 225 pytest + 42 vitest green). Docs/manifest synced
  (ARCHITECTURE, SOURCES, KOSHA_DECISIONS_NEEDED R7 note, datasets.json
  extras row). ⚠ Residual: `v0.19.0` tag pushed but the GitHub *release
  object* publish was permission-blocked — one human command:
  `gh release create v0.19.0` (notes in CHANGELOG).

- **11-07-2026 (Fable 5 `claude-fable-5`) — H691 census-§2 giants provenance sweep**
  ([H691](https://github.com/gasyoun/Uprava/blob/main/handoffs/archive/H691-Fable_kosha_local-only-giants-provenance_11.07.26.md),
  [PR #52](https://github.com/gasyoun/kosha/pull/52) merged). Every local-only giant now has
  SHA256 + live counts + schema + rebuild recipe in
  [data/manifest/datasets.json](https://github.com/gasyoun/kosha/blob/main/data/manifest/datasets.json):
  5 rows updated (corpus-lexicon, sa-ru-glossary, kosha-db, dcs-full-sqlite, samudra-corpus-db)
  + 3 new (archive-stopword-sqlite 40.57M rows, samudra-offline-packs, kosha-raw-sqlite).
  Census verdict in [DATA_LAYERS_CENSUS.md](https://github.com/gasyoun/Uprava/blob/main/DATA_LAYERS_CENSUS.md):
  only corpus_lexicon.jsonl (290 MB) is genuinely single-copy — backup pending H235.
  ⚠ Found in passing: **live kosha.db (built 05-07-2026) has NO `heritage_anchor` table** —
  the H345 heritage-witness ingest is absent from the current build; re-run the H345 ingest
  (and re-verify `/api/v1/lemma`'s `heritage` witness) next time the DB is rebuilt.

- **05-07-2026 (Sonnet 5 `claude-sonnet-5`) — Search history + analytics,
  Phase C frontend** ([H205](https://github.com/gasyoun/Uprava/blob/main/handoffs/archive/H205-Sonnet_kosha_search_history_phase_c_frontend_05.07.26.md)).
  Added [`History.svelte`](https://github.com/gasyoun/kosha/blob/main/ui/src/components/History.svelte)
  (visitor's recent searches via `GET /api/v1/history`, clear button via
  `DELETE`, magic-link email request form) and
  [`Stats.svelte`](https://github.com/gasyoun/kosha/blob/main/ui/src/components/Stats.svelte)
  (summary cards, Chart.js daily-volume line chart, top-terms table) to the
  K2b inflection UI's tab bar — both new tabs are hidden entirely when
  `window.KOSHA_API` is unset (no static-JSON fallback exists for these,
  per the plan: they're inherently live/dynamic). Added **Chart.js** as
  `ui/`'s first charting dependency. 4 new vitest component tests
  (`HistoryStats.test.js`, mocking `../lib/datasource.js`'s `API` export via
  per-test `?case=` query-busted dynamic imports — plain `vi.resetModules()`
  double-loads svelte's runtime and throws `effect_orphan`); 21/21 `ui/`
  tests green, 179/179 `pytest` green (no backend changes). Verified live
  via `uvicorn` + `vite dev`: History/Stats tabs render real data from the
  already-populated dev `kosha_history.db` (24 searches / 3 visitors),
  empty-history state, and the API-absent hidden state. Remaining 2 items
  (email provider + CORS @DECIDE, retention-purge script) moved to Next
  Steps above.

- **05-07-2026 (Sonnet 5 `claude-sonnet-5`) — Search history + analytics,
  Phases A/B (backend).** Plan:
  [H205](https://github.com/gasyoun/Uprava/blob/main/handoffs/archive/H205-Sonnet_kosha_search_history_phase_c_frontend_05.07.26.md)
  carries the remainder. New writable SQLite store
  ([`app/history_db.py`](https://github.com/gasyoun/kosha/blob/main/app/history_db.py),
  `data/db/kosha_history.db`, gitignored) kept **separate** from the
  read-only `kosha.db` so the monthly dict rebuild never touches it —
  `visitors`, `search_events`, `daily_rollup` (upserted per write, feeds
  charts without scanning raw events), `magic_links` tables.
  [`app/identity.py`](https://github.com/gasyoun/kosha/blob/main/app/identity.py):
  anonymous `kosha_anon_id` cookie (2yr, HttpOnly, SameSite=Lax), no login
  required; IPs are salted-hashed, never stored raw.
  [`app/history.py`](https://github.com/gasyoun/kosha/blob/main/app/history.py):
  `GET`/`DELETE /api/v1/history` (cookie-scoped personal history),
  `GET /api/v1/stats/{summary,timeseries,top}` (public aggregate, no cookie
  needed — designed so a future Cologne-homepage embed can fetch directly),
  and Phase-C magic-link `POST /api/v1/auth/request-link` /
  `GET /api/v1/auth/verify` scaffolding (email send is a stub pending an
  @DECIDE on a transactional-email provider, logged to GTD). `/api/v1/search`
  now logs via `BackgroundTasks` (after the response is built, so the D5-1
  latency SLO is untouched) — opens its own connection rather than reusing
  the request-scoped `Depends()` connection, since FastAPI tears that down
  before background tasks run. CORS: `allow_credentials` now follows
  `origins != ["*"]` (still safe by default; production must set an explicit
  origin list, documented in `.env.example`).
  [`tests/test_history.py`](https://github.com/gasyoun/kosha/blob/main/tests/test_history.py):
  13 new tests (per-visitor isolation, accumulation, clear, stats aggregation,
  magic-link round-trip incl. reuse-rejection), all green. Full suite
  **179/179 passed**. Verified live via `uvicorn` dev server (search → history
  round-trip, stats, magic-link stub all exercised over HTTP).

- **05-07-2026 (Opus 4.8 `claude-opus-4-8`) — Wave E1: vidyut-prakriya vs
  Cologne nominal divergence comparison.** Branch `feat/e1-dual-engine-nominal`
  → [PR #14](https://github.com/gasyoun/kosha/pull/14) →
  [v0.10.0](https://github.com/gasyoun/kosha/releases/tag/v0.10.0).
  [`scripts/compare_vidyut_cologne.py`](https://github.com/gasyoun/kosha/blob/main/scripts/compare_vidyut_cologne.py)
  (vidyut 0.4.0 `prakriya`, local — R12-clean) diffs a second engine against the
  Cologne `inflections` tables, classifying every case×number cell.
  [`E1_DIVERGENCE_REPORT.md`](https://github.com/gasyoun/kosha/blob/main/E1_DIVERGENCE_REPORT.md):
  **90.5% agreement** over 240k cells / 10k nominal stems. Divergences:
  ṇatva bug ([MWinflect#6](https://github.com/sanskrit-lexicon/MWinflect/issues/6),
  **89 stems in top-10k — blast radius > the documented 69**), pronominal
  stems (`sarva`) mis-modelled as nominals, cardinal numerals (`saptadaSan`)
  missing from Cologne but generated by vidyut, feminine consonant/monosyllabic
  forks (60% of DIFF, scholarly). Recommends **hybridize** (D3). Remainder
  (ruling @DECIDE + drafted-not-posted csl-inflect#10 give-back + verb
  comparison) queued in [H185](https://github.com/gasyoun/Uprava/blob/main/handoffs/H185-Opus_kosha_e1_dual_engine_ruling_05.07.26.md).
  `data/e1/` output gitignored (regenerable).

- **05-07-2026 (Opus 4.8 `claude-opus-4-8`) — P4 Wave K2b: Sanskrit inflection
  lookup UI (H183).** Branch `feat/p4-k2b-inflect-ui` →
  [PR #13](https://github.com/gasyoun/kosha/pull/13) →
  [v0.9.0](https://github.com/gasyoun/kosha/releases/tag/v0.9.0).
  - **Svelte 5 + Vite app** ([`ui/`](https://github.com/gasyoun/kosha/tree/main/ui))
    building into [`docs/inflect/`](https://github.com/gasyoun/kosha/tree/main/docs/inflect)
    (62 kB bundle), served by the existing Pages deploy at
    `gasyoun.github.io/kosha/inflect/`. Four features (roadmap Wave K3 folded in
    per MG ruling K2b-3): stem→paradigm (auto-detect Deva/IAST/SLP1 → SLP1,
    Devanagari-default case×number / verb grids with IAST/SLP1 toggle),
    paste-anything reverse box (wraps `/analyze`, `resolved_by` provenance),
    autocomplete (prefix range-seek over the shared 323k `lemmas.json`, live
    translit), dictionary cross-links (stem → in-app MW/PWG/AP90 entry +
    "show all forms" back — two silos, one tool). Mobile: in-container grid
    scroll, no horizontal body scroll.
  - **Data backend "both"** (K2b-2, [`ui/src/lib/datasource.js`](https://github.com/gasyoun/kosha/blob/main/ui/src/lib/datasource.js)):
    static pre-generated JSON by default (no live server; R1/R5/R12-clean),
    live FastAPI `/api/v1/…` when `window.KOSHA_API` set. Static stage-3
    segmentation degrades honestly (`segmentation_available:false`) — `tattvamasi`
    resolves only via the live-API path.
  - **Backend:** new `GET /api/v1/paradigm/{lemma}` + shared
    [`app/paradigm.py`](https://github.com/gasyoun/kosha/blob/main/app/paradigm.py)
    (nominal case×number / verb voice·tense·person×number grids, bridged stems
    fold); [`scripts/build_paradigms.py`](https://github.com/gasyoun/kosha/blob/main/scripts/build_paradigms.py)
    emits parity-locked paradigm + prefix-bucketed reverse-index shards
    (`--demo` committed; `--all` = full 176k attested-with-entry set deployed by
    MG out-of-band, A3).
  - **Reuse:** vendored **sanskrit-util** JS package (no new transcoder);
    Devanagari via `slp1_to_devanagari` (matra/conjunct composition) NOT the
    naive `iast_to_devanagari` (which emits रआमअ for rāma). `card_token` /
    `reverseBucket` are exact JS twins of the Python encoders. **D3 ṇatva bug
    (MWinflect#6) surfaced verbatim**, not "fixed" in the frontend.
  - **Tests:** +6 pytest ([`tests/test_paradigms.py`](https://github.com/gasyoun/kosha/blob/main/tests/test_paradigms.py),
    endpoint + static-shard byte-parity) → **167 passed**; 17 vitest
    (translit auto-detect, token parity, prefix seek, static data-path
    integration, full App e2e — all against the committed demo shards). Pages
    tier: `docs/inflect` = 2.0 MB → total ~404 MB, ~60% headroom under the 1 GB
    soft limit unchanged.

- **05-07-2026 (Opus 4.8 `claude-opus-4-8`) — P4 Wave K2a: reverse-lookup
  pipeline + verb ingest + stem bridge (H181).** Branch
  `feat/p4-k2a-reverse-lookup`.
  - **Verb ingest.** Fixed the upstream MWinflect Py2 bug (`clean.py`
    parenthesized-tuple lambda param → unpack in body), unblocking
    `verbs/redo.sh` → `verbs/pysanskritv2/tables/calc_tables.txt` (7,460 rows).
    [`scripts/build_inflections.py`](https://github.com/gasyoun/kosha/blob/main/scripts/build_inflections.py)
    `build_verb_inflections()` ingests present-system conjugations verbatim (D3):
    **+67,140** rows, new `person`/`tense`/`voice` columns (NULL for nominals),
    `model`=`v_<gana>`/`v_p`. Total `inflections` = 6,916,522. All 683 verb
    roots map to an entry. Fix prepared as an on-its-merits upstream PR to
    [sanskrit-lexicon/MWinflect](https://github.com/sanskrit-lexicon/MWinflect)
    (D5).
  - **Stem bridge.** [`scripts/build_stem_bridge.py`](https://github.com/gasyoun/kosha/blob/main/scripts/build_stem_bridge.py)
    → `stem_bridge` table (380 strong→weak mappings). Data-gated rule (nt→t /
    drop-final-n, only when the two spellings share a surface form). Exit case
    `Bagavant → Bagavat` unifies to one lemma; conservative (`Atman`/`Atma` NOT
    merged — no shared form).
  - **Reverse-lookup cascade.** [`app/reverse_lookup.py`](https://github.com/gasyoun/kosha/blob/main/app/reverse_lookup.py):
    `inflections` → `forms` → **vidyut-cheda segmentation**
    ([`app/segmenter.py`](https://github.com/gasyoun/kosha/blob/main/app/segmenter.py),
    vidyut 0.4.0, local vendored `data/vidyut/`, R12-clean, graceful degradation)
    behind `/api/v1/forms/{form}/analyze`, each result carrying `resolved_by` +
    `canonical_slp1`. Roadmap exit forms all resolve to entries; `tattvamasi`
    genuinely exercises stage 3 (→ `tattvam`+`asi`). **Documented deviation:**
    `dharmakSetre` resolves at stage 1 (`Darmakzetra` is a Cologne stem with
    entries), not via segmentation as the brief assumed — still meets the
    "resolve to entries" bar.
  - **Tests:** [`tests/test_reverse_lookup.py`](https://github.com/gasyoun/kosha/blob/main/tests/test_reverse_lookup.py)
    new; stale K1 verb-out-of-scope test updated. Full suite **161 passed**
    (the earlier `test_docs_site.py` staleness failure is resolved on `main`).
    Docs: [`data/SOURCES.md`](https://github.com/gasyoun/kosha/blob/main/data/SOURCES.md)
    P4-K2a section (incl. ṇatva caveat). Released as v0.8.0.

- 2026-07-03: **P2 Pages deploy LIVE** (MG-ordered, executed by the coordinating Fable 5 `claude-fable-5` session): `build_static_cache.py` full run (50,355/50,355 cards, 13.2 MB lemmas.json, attested_keys), tier committed to `main` per MG decision (`7f2f416d`; gitignore rules dropped, root `.nojekyll` + `index.html` redirect `dd0d2942`). Live-verified: docs-site 200, bhū card 23 results, lemmas.json 13,240,495 B, attested_keys 676 KB, root 200. samskrtam.ru deploy remains MG's (A3). ⚠ Measured tier size: cards = **402 MB** (D5-3 estimated ~155 MB — post-v0.7.0 heritage/evidence/inflection payloads fattened cards 2.6×); Pages soft limit 1 GB → ~60% headroom left — re-measure before the next bulk ingest.
 (Recent only)

- **03-07-2026 (Sonnet 5 `claude-sonnet-5`) — P3 evidence layer, landed via
  [PR #9](https://github.com/gasyoun/kosha/pull/9) →
  [v0.7.0](https://github.com/gasyoun/kosha/releases/tag/v0.7.0).** Full
  IMPLEMENTATION_PLAN.md P3 exit
  check passing: `dharma` shows band 1 + count_all 11,534 + rank_all 36 +
  first_era "Vedic" + ≥1 attested example
  (`tests/test_evidence.py::test_dharma_evidence_band_counts_example`);
  `/api/v1/search` ranking measurably changes for the frozen 20-headword
  sample (≥50% of multi-result queries reorder vs. alphabetical,
  `test_search_ranking_measurably_changes_for_sample`); every evidence badge
  carries a `source` string (`test_evidence_provenance_label_on_every_badge`).
  Built on P1's existing frequency LEFT-JOIN instead of a new `evidence`
  table (redundant by 03-07-2026 — see
  [`scripts/build_evidence.py`](https://github.com/gasyoun/kosha/blob/main/scripts/build_evidence.py)
  docstring for the full reasoning, band thresholds, and the deliberate
  per-lemma (not per-sense) scope of `corpus_lexicon.jsonl` examples: the
  feed has no sense-level tagging).
  [`app/evidence.py`](https://github.com/gasyoun/kosha/blob/main/app/evidence.py)
  shapes the block; mirrored into
  [`scripts/build_static_cache.py`](https://github.com/gasyoun/kosha/blob/main/scripts/build_static_cache.py)
  so P2's static cards stay byte-identical to the live API. Fail-closed
  negative case tested: a band-5 (no-DCS-signal) lemma renders `count_all:
  null` / `example: null`, never a fabricated `0` or invented citation;
  `genre` is honestly `null` (not derivable from the current DCS
  extraction — only a chronological period vector is stored, no per-genre
  breakdown). Band distribution on the live spine: 1=493, 2=1,441, 3=7,484,
  4=51,922, 5=262,085; 38,595 lemmas got a corpus example out of 1,091,528
  `corpus_lexicon.jsonl` rows scanned. Full suite green (pre-existing,
  unrelated `test_docs_site.py::test_committed_output_is_current` staleness
  failure untouched — not caused by this change).

- **03-07-2026 (Sonnet 5 `claude-sonnet-5`) — P4 Wave K1: Cologne inflection
  tables ingest + JSON analysis endpoint, landed via
  [PR #9](https://github.com/gasyoun/kosha/pull/9) →
  [v0.7.0](https://github.com/gasyoun/kosha/releases/tag/v0.7.0).** New
  `inflections` sidecar table
  in [`scripts/build_db.py`](https://github.com/gasyoun/kosha/blob/main/scripts/build_db.py)
  SCHEMA + `--stage inflections` dispatch, loaded by new
  [`scripts/build_inflections.py`](https://github.com/gasyoun/kosha/blob/main/scripts/build_inflections.py)
  from the sibling `MWinflect` checkout's
  `nominals/pysanskritv2/tables/calc_tables.txt` (Cologne csl-inflect's own
  data, engine=Cologne verbatim per D3 — not vidyut, that's Wave E1). New
  read-only endpoint
  [`GET /api/v1/forms/{form}/analyze`](https://github.com/gasyoun/kosha/blob/main/app/main.py)
  returns every grammatical parse (lemma/model/gender/case/number/refs) for
  a form, same envelope convention as the other v1 endpoints. Tests:
  [`tests/test_inflections.py`](https://github.com/gasyoun/kosha/blob/main/tests/test_inflections.py)
  (6 new, all green) hand-verify the roadmap's three exit-test forms:
  `bhagavAn` → nom-sg (m_vat), `rAmeRa` → instr-sg (m_a), `dharmakSetre` →
  correctly surfaces **both** the loc-sg parse (m_a and n_a) and the
  nom/acc/voc-du (n_a) parse — genuine Sanskrit ambiguity, not collapsed to
  one answer.
  **Build chain result:** `nominals/redo.sh` (Python 3, needed a
  `python3`→`python` shim on PATH, none pre-existed) ran clean:
  288,844 stems → `calc_tables.txt` (96 MB) → ingested as 6,849,382
  `(form,lemma,model,case,number)` rows, 3,267,305 distinct forms, 222,955
  distinct lemmas, ~2 min. **`verbs/redo.sh` is BLOCKED upstream**:
  `verbs/pysanskritv2/inputs/clean.py` line 76 uses Python-2-only
  parenthesized-tuple lambda-parameter syntax
  (`lambda (c,v): ...`, removed in Python 3), which cascades — `bases/` and
  `tables/` then fail on their now-missing inputs. This is a genuine
  upstream MWinflect defect (not touched, not silently patched — flagged
  here and worth a small upstream PR at some point, out of scope for this
  session); it does **not** block K1's own exit bar since none of the three
  roadmap exit-test forms are verb forms. K1 therefore ships **nominal
  declensions only**; `vlgtab1`/`vlgtab2`-equivalent verb conjugations are
  future work once the upstream syntax bug is fixed (either here or via a
  csl-inflect PR). Full suite: 149 passed, 1 pre-existing unrelated failure
  (`test_docs_site.py::test_committed_output_is_current`, docs-site staleness
  from the parallel Wave-3 docs-site work already in flight, not touched by
  this session).
  **Known data caveat inherited from Cologne, ingested as-is per D3:** the
  pre-existing Dev Note above (H094 sweep) documents a live ṇatva bug in
  csl-inflect's m_a declensions for ~69 compounds (nṛpa, turaga, uraga…,
  [MWinflect#6](https://github.com/sanskrit-lexicon/MWinflect/issues/6)) —
  e.g. it emits `nfpena`/`nfpAnAm` where attested Sanskrit is
  `nfpeRa`/`nfpARAm`. K1 ingests `calc_tables.txt` verbatim (D3 = Cologne
  tables as-is, no local correction layer), so those specific cells carry
  the bug through into `inflections` unfixed. A correction pass (either an
  upstream MWinflect PR or a local override table) is future work, not K1
  scope.

- **03-07-2026 (Sonnet 5 `claude-sonnet-5`) — H111: Heritage/INRIA forms
  ingested as a third, low-trust `forms` witness. Branch
  `h111-heritage-forms-ingest` → [PR #7](https://github.com/gasyoun/kosha/pull/7)
  merged, released as
  [v0.6.0](https://github.com/gasyoun/kosha/releases/tag/v0.6.0).** `forms`
  gains a nullable `category` column
  (auto-migrated for existing DBs in
  [`scripts/build_db.py`](https://github.com/gasyoun/kosha/blob/main/scripts/build_db.py));
  [`scripts/build_forms.py`](https://github.com/gasyoun/kosha/blob/main/scripts/build_forms.py)
  loads the sibling
  [`heritage_only_forms.tsv`](https://github.com/gasyoun/SanskritLexicography/blob/master/HeadwordLists/heritage_only_forms.tsv)
  (992,194 rows,
  [SanskritLexicography PR #100](https://github.com/gasyoun/SanskritLexicography/pull/100))
  as `source='heritage'`, loaded LAST, additive-only. Verified: `dcs` 397,843
  / `vidyut` 28,567 unchanged; `heritage` +951,991 rows (40,203 feed rows
  collide on the `(form_slp1,lemma_slp1,source)` PK across differing
  `category` — `category` isn't in the PK, documented as a minor precision
  loss on the down-weighting lever, no coverage loss). Trust ordering
  `dcs > vidyut > heritage` documented in ARCHITECTURE.md, `build_forms.py`,
  the `/api/v1/form` handler, `data/SOURCES.md`, and
  [KOSHA_DECISIONS_NEEDED.md](https://github.com/gasyoun/kosha/blob/main/KOSHA_DECISIONS_NEEDED.md).
  118/118 tests green. The 6,966-row nominal disagreement pool (DCS-correction
  candidates) is explicitly out of scope — flagged for a future handoff.

- **03-07-2026 (Opus 4.8 `claude-opus-4-8`) — P2 static-cache generator, branch
  `feat/p2-static-cache` → release 0.5.0.** First agent-doable P2 slice, built to
  the fixed D5-3 targets (H085).
  [`scripts/build_static_cache.py`](https://github.com/gasyoun/kosha/blob/main/scripts/build_static_cache.py)
  emits (1) **50,355** frequency-ranked sharded per-lemma cards
  (`count_all IS NOT NULL` ∧ ≥1 entry — count verified exactly against the DB),
  each **byte-identical** to `/api/v1/lemma` (reuses `app/` render/scan/translit,
  no reimplementation; parity + encoding locked by 8 new tests, **115/115** green);
  (2) the ~13 MB autocomplete index (all 323,425 lemmas → `docs/js/data/lemmas.json`,
  the INDEX per D5-3a) + a `attested_keys.json` sidecar; (3) an opt-in full-222k
  tarball (R1c/R4). Case-safe `card_token` filename encoding with a documented JS
  twin ([docs/README.md](https://github.com/gasyoun/kosha/blob/main/docs/README.md)).
  Generated output gitignored — **MG runs the generator + deploys** (A3;
  `/publish-safety-check` first). Throughput ~144 cards/s (~6 min full set);
  builds never touch a live service (R12).

- **03-07-2026 — MG enabled GitHub Pages** on
  [gasyoun/kosha](https://github.com/gasyoun/kosha/settings/pages) — one of
  the two P2 human gates (A3) is done; only the samskrtam.ru deploy remains.

- **03-07-2026 (Fable 5 `claude-fable-5`) — inflection roadmap + MG interview.**
  Audit of the Cologne inflected-form tool
  ([live](https://sanskrit-lexicon.uni-koeln.de/scans/csl-inflect/web/index.php),
  repo [csl-inflect](https://github.com/sanskrit-lexicon/csl-inflect) — Jim
  Funderburk, dormant since 07-2023) → 6 rulings elicited (hybrid venue ·
  full scope · Cologne tables first, vidyut evolution · P4 slot · probe-first
  PRs · research ALL question issues) →
  [ROADMAP_INFLECT_2026_2027.md](https://github.com/gasyoun/kosha/blob/main/ROADMAP_INFLECT_2026_2027.md)
  authored; handoffs H093/H094 minted in Uprava; GTD wired.

- **03-07-2026 (Opus 4.8 `claude-opus-4-8`) — D5 measure-then-decide, branch
  `feat/phase1-d5-measure` → release 0.4.0.** Last Phase-1 step. **107/107
  tests still green.** Full numbers:
  [D5_MEASUREMENTS.md](https://github.com/gasyoun/kosha/blob/main/D5_MEASUREMENTS.md);
  decisions:
  [KOSHA_DECISIONS_NEEDED.md](https://github.com/gasyoun/kosha/blob/main/KOSHA_DECISIONS_NEEDED.md).
  - **Latency bug found + fixed (the headline).** `/api/v1/lemma` scanned all
    ~286k MW rows (planner seeked only `dict=` via the `UNIQUE(dict,L)`
    autoindex, which also gave free `ORDER BY L`; `entries_key(slp1_key)` was
    ignored) — **~240 ms/lookup**. Covering index `entries(dict, slp1_key, L)`
    (+ `ANALYZE`) fixes it to **~0.3 ms** query / **10–20 ms** handler / 22–31 ms
    e2e. In [`scripts/build_db.py`](https://github.com/gasyoun/kosha/blob/main/scripts/build_db.py)
    schema; the SLO assumes it.
  - **Decisions (judgment tier, from data):** SLO p50<20 / p95<100 / p99<250 ms
    server-side; rebuild **change-triggered ~monthly, not nightly** (nightly
    mints needless citable `data_version`s — R1 tension); static-cache =
    **~50,355 attested-with-entry lemmas, sharded per-lemma (~155 MB),
    frequency-ranked** (single bundle rejected — 100 MB/file cap at ~33k).
  - **R3 fallback now a tested path** —
    [`scripts/fallback_csl_orig.py`](https://github.com/gasyoun/kosha/blob/main/scripts/fallback_csl_orig.py)
    parses csl-orig `ap90.txt` directly: **100% inventory parity** (34,882
    records, every L/`<k1>`/`<pc>`). Honest boundary: bodies are upstream
    display markup, so a render()-able fallback also needs csl-orig→XML
    `make_xml` (documented, not glossed).
  - **`sources.csl_orig_commit` provenance RESOLVED** (was flagged) by offline
    cross-dating vs the local csl-orig commit log: mw `392ed6b`, pwg `8822922`,
    ap90 `51232f2` (upper bound, labelled). Wired into
    [`scripts/build_entries.py`](https://github.com/gasyoun/kosha/blob/main/scripts/build_entries.py),
    applied to the DB, surfaced by `/api/v1/meta`.
  - **Decisions doc relocated** SanskritLexicography→kosha (canonical home; it
    never existed at the referenced SL path, which is the banner-flagged
    planning repo); doc links repointed. MG can reverse.
  - **Measurement variance noted** honestly (shared Windows box; render micro-
    bench re-run clean after a transient-load 3× spike).

- **03-07-2026 (Opus 4.8 `claude-opus-4-8`) — D4 follow-on, branch
  `feat/phase1-d4-followon` → release
  [0.3.0](https://github.com/gasyoun/kosha/blob/main/CHANGELOG.md).** Closed the
  three pieces PR [#2](https://github.com/gasyoun/kosha/pull/2) deferred.
  **107/107 tests green** (was 20).
  - **Full `render()` port** ([`app/render.py`](https://github.com/gasyoun/kosha/blob/main/app/render.py)):
    code-level faithful port of the mw/pwg/ap90 path of csl-websanlexicon's
    canonical `basicdisplay.php` (SAX engine, reimplemented with expat) +
    relevant `basicadjust.php` passes; returns basicdisplay's `row` (prose HTML),
    `row1` metadata excluded by design. Two documented deviations: server-side
    `<s>` SLP1→IAST via sanskrit-util (not client-JS `<SA>`); no DB tooltips /
    external `<ls>` hrefs (ls_resolver.py D3 follow-on, RISKS R12). **38 frozen
    checksummed golden snapshots** (mw 14 incl. banD/akṣa, pwg 12, ap90 12;
    ≥10/dict), seeded-selected (EVAL_PLAN §0) — locks against regression, not
    diffed vs the live PHP stack (stated honestly).
  - **Per-dict sense segmentation** ([`app/segment.py`](https://github.com/gasyoun/kosha/blob/main/app/segment.py)):
    `<div>`-division rule (MW to/vp, PWG numbered, AP90 bold-numbered); MW
    303,022 · PWG 223,446 · AP90 165,935 senses; markerless entries keep the
    single-sense fallback. senseN = stable within-entry counter (A2).
  - **R1 citability C1–C2** ([`app/cite.py`](https://github.com/gasyoun/kosha/blob/main/app/cite.py),
    [`app/versions.py`](https://github.com/gasyoun/kosha/blob/main/app/versions.py),
    [`scripts/archive_senses.py`](https://github.com/gasyoun/kosha/blob/main/scripts/archive_senses.py),
    [`scripts/build_crosswalk.py`](https://github.com/gasyoun/kosha/blob/main/scripts/build_crosswalk.py)):
    `cite` carries browser-resolvable `resolution_url` + durable `release_asset`
    + BibTeX/CSL-JSON; `/api/v1/sense/{id}@version` resolves old citations from
    archived dumps (**T-UC10** passes; unarchived → 404 w/ pointer); rebuilds
    emit `sense_crosswalk.tsv` (SPLIT/MERGED/GONE/MOVED, zero-cost unchanged).
    Verified on real PWG data. Fixed a real bug found in testing:
    `parse_sense_id`'s greedy regex ate the `@version` on dotted versions —
    now splits on `@` first.
  - **Two smaller items left flagged** (see Next Steps): `csl_orig_commit`
    provenance; PWG multi-volume scan (probed — server tolerates `vol=` but
    honoring undetermined).

- **02-07-2026 (Sonnet 5 `claude-sonnet-5`) — Phase 1 D1–D4 execution, branch
  `feat/phase1-d1-d4`.** Full data pipeline + API against real data, not
  planning docs. Every number below and every deviation from
  [PHASE1_PLAN.md](https://github.com/gasyoun/kosha/blob/main/PHASE1_PLAN.md)'s
  stated figures is recorded in
  [`data/SOURCES.md`](https://github.com/gasyoun/kosha/blob/main/data/SOURCES.md)
  per EVAL_PLAN's anti-gaming rules.
  - **D1:** [`scripts/build_db.py`](https://github.com/gasyoun/kosha/blob/main/scripts/build_db.py)
    loads `union_headwords.tsv` (323,425 rows, not 323,426 — plan's stated
    count was off-by-one, corrected here) into `lemmas`, LEFT-JOINs the
    frequency sidecar (61,340 overlap by key, 51,773 with `count_all`).
  - **D2:** [`scripts/build_entries.py`](https://github.com/gasyoun/kosha/blob/main/scripts/build_entries.py)
    — the NEW per-dict `<pc>` dispatcher — loads mw/pwg/ap90 from csl-sqlite
    release `2026-06-28-08-27-31` (286,525/123,366/34,882 entries, 100 % pc
    coverage all three). 10/10 spot-verify checks pass incl. the two named
    ones (MW L142512=banD@720,1, L523=akza@3,2). Sense table seeded with the
    ARCHITECTURE-sanctioned single-sense fallback (real per-dict segmentation
    deferred, see Next Steps).
  - **D3:** [`scripts/build_forms.py`](https://github.com/gasyoun/kosha/blob/main/scripts/build_forms.py)
    loads 426,410 form→lemma pairs from `gasyoun/SanskritRussian`
    (`dcs_form2lemma.tsv` + `vidyut_form2lemma.tsv`); `bhagavān→bhagavant`
    check passes. [`app/scan_resolver.py`](https://github.com/gasyoun/kosha/blob/main/app/scan_resolver.py)
    implements the real Cologne `servepdf.php` mechanism
    ([`COLOGNE/api/servepdf.md`](https://github.com/sanskrit-lexicon/COLOGNE/blob/master/api/servepdf.md))
    directly — `ls_resolver.py` (named in the plan) turned out to resolve
    `<ls>` citation cross-references, an unrelated mechanism; documented in
    SOURCES.md. 10/10 sampled scan URLs live-verified HTTP 200.
  - **D4:** full kosha API v1 (`/api/v1/{lemma,form,search,sense,meta}` +
    `/health`) plus the two Salt facade REST faces
    ([`app/salt.py`](https://github.com/gasyoun/kosha/blob/main/app/salt.py),
    id-minting ported directly from `csl-apidev/api1/salt_common.php`'s real
    PHP logic — no live csl-apidev instance exists to diff against, confirmed
    dev-only in that repo's own docs). Caught and fixed a real bug during
    build: naive `<hom>` text matching false-positived on MW's `ka`
    decimal-lnum sub-entries; fixed to the entry's own `<info hui="N"/>`
    attribute, re-verified distinct across all 31 real `ka` records.
    Salt-face parity check (agni=10, indra=17 — matches csl-apidev's
    documented count exactly, ka=31 incl. the 4 doc-cited `hui`-suffixed
    ids) passes against real MW data.
    [`app/render.py`](https://github.com/gasyoun/kosha/blob/main/app/render.py)
    ships a **partial** subset only — not the full 5,516-line
    basicadjust.php/basicdisplay.php port ARCHITECTURE.md A1 specifies; no
    golden snapshots yet; flagged as the largest remaining gap, not silently
    claimed done. [`tests/test_api.py`](https://github.com/gasyoun/kosha/blob/main/tests/test_api.py):
    **20/20 green** locally.
  - **Not done, flagged honestly (see Next Steps):** full render() port,
    RISKS.md R1 citability commitments (version-resolution URL,
    `sense_crosswalk.tsv` — both trigger at the first real data release, not
    this dev build), per-dict sense segmentation, csl_orig_commit
    provenance, PWG multi-volume scan disambiguation.

- **02-07-2026 (Opus 4.8 `claude-opus-4-8`) — Bridge 1: frequency sidecar layer.**
  Built
  [`data/frequency/build_frequency_layer.py`](https://github.com/gasyoun/kosha/blob/main/data/frequency/build_frequency_layer.py)
  joining VisualDCS `archive.sqlite` (M9) `period_freq` / `core_vocab` into
  [`lemma_frequency.tsv`](https://github.com/gasyoun/kosha/blob/main/data/frequency/lemma_frequency.tsv)
  (83,277 SLP1-keyed rows: `count_all` + per-period vector + `coverage_pct`).
  Sidecar not column — kosha has no data layer yet; D1/D2 LEFT-JOIN it. Validated
  73.7 % overlap with `union_headwords`; unmatched = DCS preverb/causative lemmas.
  Feed also drives the pwg_ru slice ordering (cross-checked vs `dcs_freq.json`,
  99.4 % co-present, 0.72 adjacent concordance). Registered in `PROJECT_INTERLINKS.md`.
- **02-07-2026 (Fable 5 `claude-fable-5`) — README drastic rewrite, released as
  [v0.2.1](https://github.com/gasyoun/kosha/releases/tag/v0.2.1):** layered
  dual-audience structure (public pitch + pre-alpha banner + 18-question FAQ on
  top; engineering spine under "For contributors & agents"). MG choices: both
  audiences layered · brand-led H1 · all four FAQ angles · prominent status
  banner. Presentation only — no decisions changed.
- **02-07-2026 (Fable 5 `claude-fable-5`) — judgment layer complete, released
  as [v0.2.0](https://github.com/gasyoun/kosha/releases/tag/v0.2.0):**
  [EVAL_PLAN.md](https://github.com/gasyoun/kosha/blob/main/EVAL_PLAN.md)
  (8 anti-gaming rules; G-SEG/G-RENDER/G-SALT/G-PC/G-SCAN/G-LAT gates;
  T-UC1…T-UC13) ·
  [RELATIONS.md](https://github.com/gasyoun/kosha/blob/main/RELATIONS.md)
  (Meyer draft, Cologne framing, upstream-vs-track-3 table, contact registry) ·
  [RISKS.md](https://github.com/gasyoun/kosha/blob/main/RISKS.md) (R1–R12
  pre-mortem; 4 citability commitments). P1 execution is now fully gated.
- **02-07-2026, all by Fable 5 (`claude-fable-5`), single day:** repo created
  (Phase-1 go by MG) → POSITIONING + SUMMARY (rebrand: **Gasuns Sanskrit
  Dictionary**; three-track identity; MG UI override recorded) →
  [COMPARISON.md](https://github.com/gasyoun/kosha/blob/main/COMPARISON.md)
  12-platform live survey (3 research agents, same-day fetches; michaelmeyer.fr
  = 41 dicts w/ per-sense scan links — positioning corrected) →
  [ARCHITECTURE.md](https://github.com/gasyoun/kosha/blob/main/ARCHITECTURE.md)
  A1–A4 + SQLite DDL + API v1 + Salt max-reuse (required, incl. facade REST in
  D4) → licenses locked → full
  [IMPLEMENTATION_PLAN.md](https://github.com/gasyoun/kosha/blob/main/IMPLEMENTATION_PLAN.md)
  P1–P7 with exit checks + MG critical path. Origin story: same-day triage of
  the fabricated planning corpus in SanskritLexicography
  ([v0.0.34](https://github.com/gasyoun/SanskritLexicography/releases/tag/v0.0.34)).

_Dr. Mārcis Gasūns_
