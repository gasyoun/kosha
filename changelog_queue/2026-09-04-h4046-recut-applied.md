- **H4046 (OxAlpha `zai-coding-plan/glm-5.3-flash`) — re-cuts applied after MG's 3/3 approve vote on the regen-recuts sheet ([#522](https://github.com/gasyoun/kosha/pull/522); [vote sheet](https://gasyoun.github.io/vote/sheets/h4046_regen_recuts_3.html)):** D1 `data/sandhi/sandhi_curriculum.tsv` re-cut 2,181 → **2,634 rules** (builder unchanged since H902; 0 rules lost, +453 new on today's `corpus_sandhi.tsv`; cascade sandhi-drills 396 → **399** across tsv/json/apkg); D3 `data/gita/gita_inflection_divergences.tsv` re-cut 1,279 → **661** against the verified rebuilt kosha.db (agreement 93.0 → 98.7%); D2 `data/xref/dcs_cdsl_xref_v2__pending-upstream.tsv` built and verified — **98,606 attested lemmas, 56,377 linked (57.2%)** (v1: 15,902 / 81.4% on the pre-conllu-rewrite master) — the upstream csl-apidev commit stays QUEUED behind the Cologne fence (direct PRs forbidden; GTD `@DO` on the batch-PR path). Manifest rows updated (rows + size_bytes + `regen_checked` → «VOTED re-cut applied») for the four affected datasets.
