# H4019 follow-up (MSI box): UTF-8 opens for the sense-dating build/hydrate

**Date:** 04-09-2026 · **Tier:** OxAlpha `zai-coding-plan/glm-5.3-flash` · **Box:** MSI (Windows, cp1251 locale)

`scripts/build_sense_dating.py` and `app/dating_hydrate.py` read their UTF-8
inputs (`works_hand.tsv`, `abbrev_map.tsv`, `dcs-text-dates-2021.tsv`,
`sense_corpus_concordance.tsv`, the two evidence snapshots) with bare
`open()` / `read_text()` — locale-encoded. On the MSI box the locale is
cp1251, so every non-ASCII work name (ā, ṛ, Devanagari) mangled on read:
the hand-verified canon_dm join missed `ramayana` (`--check` exit 1) and the
H4026 badge hydrate resolved 0 citations (hits==0). Linux/Mac CI never saw
it because their locale is UTF-8. Fix: explicit `encoding="utf-8"` on every
read/write in the two files (17 call sites). `tests/test_sense_dating.py`
17/17 green on the MSI box after the fix.

Found during the GTD pin-reconcile MSI half (sanskrit-util `e147349` /
0.11.0 install + suite run), same row as the 0.117.12 entry.
