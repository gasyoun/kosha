- **Maintenance (OxAlpha `zai-coding-plan/glm-5.3-flash`) — sanskrit-util pin moved `9ac617a2` (0.4.1) → [`e147349`](https://github.com/sanskrit-lexicon/sanskrit-util/pull/72) (0.11.0): both kosha supply paths now carry the word-final nasal fold.** The GTD row opened during the H4026 pass asked for the reconcile; the сверка (04-09-2026) measured the pin at 0.4.1 with `form_key('rasaṃ') → 'rasan' ≠ 'rasam'` — and the part the row missed: the Mac sibling checkout (`a2c4714`, 0.10.0) ALSO lacked the fold, so [scripts/rebuild_a3_chain.py](https://github.com/gasyoun/kosha/blob/main/scripts/rebuild_a3_chain.py) would refuse to start on this box (its precondition asserts `rasaṃ == rasam` before stage 1). The bump aligns the pip-pin with the 0.11.0 semantics the H3911/H3925 A3 chain is keyed on; `requirements.lock.txt` carries no sanskrit-util row (git-direct dep — untouched). The vendored [ui/src/lib/sanskrit-util.mjs](https://github.com/gasyoun/kosha/blob/main/ui/src/lib/sanskrit-util.mjs) is re-copied from `js/index.mjs` @ `e147349` (the fix touched the JS port and vectors too). Full suite on the Mac box against the new pin: **619 passed, 225 skipped** — identical pass/skip profile to the 03-09 H4026 baseline (skips = full-data tier needing the 4 GB DB); sibling checkout now at `e147349` (0.11.0 probed). MSI-box half (sibling ff-update + suite there) stays on the GTD row.
