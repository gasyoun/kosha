- **H4735 (OxAlpha `opencode/z-ai/glm-5.3-flash`) — Diachronic sense portraits: kosha-sense-frequency × sense-dating. Census C8 / sibling B11 lands the per-sense frequency × first-era join.** [sense_portraits.tsv](data/dating/sense_portraits.tsv) (7,349 rows, 549,303 bytes) joins every [sense-dating](data/dating/sense_dating.tsv) per-sense first-attestation era bucket (PWG v1 500-headword sample, H4019) with the [kosha-sense-frequency](data/frequency/sense_frequency.tsv) MW-layer per-sense counts, bridged through the H3744 crossdict pilot's MW inventory column — the only existing bridge between PWG concordance sense ids (`1a`) and MW sense numbers (`A#12`). Lossless LEFT JOIN: all 7,349 dated senses carried, era buckets never altered; `bridge_status` keeps every row's provenance (`joined` 273 / `bridge_no_freq` 223 / `bridge_unaligned` 6,853 / `no_pilot_row` 0). HONESTY STAMPS carried in output and report: first_era = first attestation among the works PWG itself cites, NOT the origin of the meaning (H4019 preface caveat); the bridge is the pilot's MW *inventory* column, not a sense alignment (H3744 note verbatim) — a joined row means "this PWG concordance sense's inventory MW sense has a DCS frequency row", nothing stronger. Era census (recount-verified): vedic 1,342 / epic-sutra 2,184 / classical 1,107 / early-medieval 1,039 / late-medieval 652 / null-undateable 1,025. Per-sense frequency coverage is 273/7,349 = 3.7% — bounded by the pilot inventory fill (497 of 7,359 pilot rows carry an MW id); widening it is the concordance→MW alignment programme, not this join; feeds sense-resolved LSC (A57) with the era axis × frequency weight. Verify: [build script](scripts/build_sense_portraits.py) `--check` era-bucket recount (independent Counter recount from the INPUT dating table), lossless row parity, joined-payload recount vs the frequency sidecar — PASS, exit 0, deterministic (0.8 s, no network). Report: [SENSE_PORTRAITS_REPORT.md](data/dating/SENSE_PORTRAITS_REPORT.md). Manifest row `sense-portraits` (tier public, README count 129→130). Edge registered in Uprava `interlinks_edges.tsv` (kosha→sense-resolved LSC lane).
