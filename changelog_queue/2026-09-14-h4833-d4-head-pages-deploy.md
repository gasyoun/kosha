- **H4833 (OxAlpha `deepseek/deepseek-v4-flash`) — the D4 static word-page head is committed to the Pages surface (10,370 pages), and the Bühler glossary consumer's landing-page coverage goes 48 % → 75 %.** [`build_word_pages.py`](scripts/build_word_pages.py) `--coverage 0.95` (the D4 default; N re-measured at build time: **11,148 lemmas / 95.00 %**; head∩cards **10,370**) rendered into the repo-root `w/` tree — the committed Pages surface, per P5-4 «prerender the top-N attested lemmas to the GitHub Pages tier» — **+8,401 new pages / 269.5 MB**; `w/` is now 10,725 files / 398.4 MB. **Determinism proof:** `--force` re-rendered the 2,324 already-committed pages **byte-identically** (`git status` = 0 modified, 8,401 untracked), so the H4026 published chrome is untouched and the delta is exactly the new head. **Consumer acceptance** (measured against [buhler-sanskrit-book](https://github.com/alexander-myltsev/buhler-sanskrit-book) `src/dictionary/*.tsv` — 639 glossary rows mapped through `card_token`): landing pages **305 → 482 (48 % → 75 %)**, card JSONs 503 (79 %); residue = 21 card-having lemmas outside the 95 % head (SSR tail) + 136 rows with no card (lexicon/keying gap, e.g. prefixed roots `A-gam`). **Budget note (MG-ruled 14-09, option A «ship + recalibrate»):** the true full-head Pages footprint is **~858 MB = 83.7 %** of the 1,024 MB cap — above the W4b gate's 70 % ceiling (716.8 MB); `measure_pages_budget.py` prints GATE PASS only because its 300-card head-band sample reads **22.79 KB/page** against the built tree's true **38.5 KB mean** (H1586-class sampling bias — the sampler is the defect; recorded in Uprava FINDINGS, recalibration minted as a follow-up). No renderer change; no DB needed (the build renders from the committed `docs/cards/` set).
