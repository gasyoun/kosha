- **H5399 (Opus 5 `claude-opus-5`) — 410 Russian glosses of the «Старт чтения» SRS lemma feed recovered from the unchanged pin: the deriving reader was dropping them, the data was never missing.** The H5285 NKRYa gloss lint in Systema-Sanscriticum ([`resources/data/nkrya_lint/lemmas_gloss_lint.tsv`](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/resources/data/nkrya_lint/lemmas_gloss_lint.tsv)) reported 841 of 1 286 rows as `status: empty` — and **838 of those 841 were every single subhāṣita-beginner row**, which is the shape of a reader bug, not of missing linguistics. It was: [`data/cohort_start_chteniya/subhashita_beginner_pack.json`](https://github.com/gasyoun/kosha/blob/main/data/cohort_start_chteniya/subhashita_beginner_pack.json) stores each chunk's Russian glosses as a **list of `{surface, lemma}` dicts parallel to the `lemma_slp1` list** (`lemma_slp1: ["Darma"]`, `gloss_ru: [{"surface": "по закону", "lemma": "дхарма"}]`), while [`scripts/freeze_cohort_start_chteniya.py`](https://github.com/gasyoun/kosha/blob/main/scripts/freeze_cohort_start_chteniya.py) called its scalar `as_str()` helper on that value — and `as_str()` returns `""` for a list of dicts. The freeze MANIFEST had been reporting 85.3% lemma-layer RU coverage on the very same file the derived TSV called empty; nobody had put the two numbers side by side. Fixed by reading the gloss at the **same index as the lemma actually used** (new module-level `aligned_pairs()` / `subhashita_gloss_index()` / `lemma_rows()` / `write_lemma_tsv()`, hoisted out of `build()` so the derivation is reusable), preferring the dictionary-form `lemma` slot over the inflected `surface` slot. Result: `lemmas_for_srs.tsv` goes from 445 to **855 of 1 286 rows glossed** (subhāṣita 0 → 410) with **zero bytes of pack content changed** — only the derived TSV and its one MANIFEST row, re-pinned by the new [`scripts/refreeze_lemmas_for_srs.py`](https://github.com/gasyoun/kosha/blob/main/scripts/refreeze_lemmas_for_srs.py), which grafts that single row onto the committed manifest for the same reason H5398 did (a full builder re-run would re-pin the sandhi assets from sources that moved since 2026-08-01 and drop the H2129 `manifest_fix` note). `python scripts/freeze_cohort_start_chteniya.py --check` → GOAL OK, 5/5 pins hash-clean. **Residual, and it is a dead end for mechanical work:** 428 subhāṣita + 3 Hitopadeśa lemmas have no Russian gloss in any source we own, and 34 Hitopadeśa rows still carry English in the Russian field; Systema's own DCS-derived [`resources/data/sa_ru_glossary.json`](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/resources/data/sa_ru_glossary.json) (26 071 entries, frequency floor 2) covers **0 of the 465** — they are precisely the rare words a frequency-floored corpus glossary omits (`autkārṣya`, `ajāgalastana`, `nagnakṣapaṇaka`, `daiva`), so closing them needs a licensed Кочергина lookup plus 10-card review sheets, not another pass over our own data.
