- **H5398 (Opus 5 `claude-opus-5`) — 107 Russian lemma glosses in the Hitopadeśa-0 reading pack put into dictionary form, and the «Старт чтения» freeze re-pinned.** The H5285 NKRYa gloss lint in Systema-Sanscriticum ([`scripts/nkrya_gloss_lint.py`](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/scripts/nkrya_gloss_lint.py), output [`resources/data/nkrya_lint/lemmas_gloss_lint.tsv`](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/resources/data/nkrya_lint/lemmas_gloss_lint.tsv)) flagged 96 `tokens[].gloss_ru.lemma` values in [`reading/data/hitopadesa-0.json`](https://github.com/gasyoun/kosha/blob/main/reading/data/hitopadesa-0.json) as inflected word-forms rather than dictionary forms — «сближении» for `abhigamana`, «голову» for `śiras`, «чертит» for `likh`. In the SRS deck the lemma slot is shown as a standalone flashcard answer, so an oblique case there is simply wrong. Each was replaced by the pymorphy3 lemma the lint proposed (mechanical and reversible, grill 23-09-2026 Q2 — no vote); **11 further `inflected_ambiguous` rows** were decided by hand from the Sanskrit part of speech (verb root → infinitive, noun/adjective → nominative): `han`/`nihan` «убит» → «убить», `nidhā` «сложенные» → «сложить», `svap` «спит» → «спать», `stha` «стоит» → «стоять», `mṛga` «зверей» → «зверь», `sumanas` «цветами» → «цветок», `toya` «воду» → «вода», `tīra` «берегу» → «берег», `vivekin` «отличен» → «отличный», `vādin` «говорящие» → «говорящий». 174 tokens rewritten across 107 distinct lemmas; `surface` and `root` gloss slots, the EN `gloss`, and every `form`/`lemma`/`upos`/`morph`/`slp1`/`href`/`tier` field are byte-identical. The H2109 freeze under [`data/cohort_start_chteniya/`](https://github.com/gasyoun/kosha/tree/main/data/cohort_start_chteniya) was re-pinned for the two affected assets only — `hitopadesa-0.json` and the derived `lemmas_for_srs.tsv` — by grafting the rebuilt rows onto the committed MANIFEST rather than re-running the whole builder: a full re-run would also re-pin the two sandhi assets from live sources that have moved since 2026-08-01 (rule count 35,533 → 45,296) and would drop the H2129 `manifest_fix` note, and re-freezing the sandhi band is a separate decision. `python scripts/freeze_cohort_start_chteniya.py --check` → GOAL OK, all five pins hash-clean. Downstream: Systema-Sanscriticum re-vendors the pack via `scripts/vendor_cohort_start_chteniya_packs.py`.
