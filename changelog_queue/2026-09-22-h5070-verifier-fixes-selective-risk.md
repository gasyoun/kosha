- **H5070 follow-up (Claude Opus 5 `claude-opus-5`) — Astra review FAIL fixed: the selective-risk canary was identifiable in the deck TSV (only card with `stratum_eligible=0`).** The sampler now dresses the control in the metadata of the stratum it imitates; the frozen 20-09 deck still reproduces byte-for-byte with `--legacy-canary-metadata`. A blind second adjudicator (Sonnet 5 `claude-sonnet-5`, rendered cards only, never told a control existed) caught the canary as `different` and agrees with adjudicator 1 on 59/60 cards (κ 0.956). C014 relabelled `unsure` (two compositional stubs); strict wrong-match now 18.4 % / 16.4 % / 7.4 % at all-eligible / ≥0.40 / ≥0.70, estimated over the 6,860 gold-excluded eligible rows (not all 7,087 aligned). Intervals re-described as a heuristic envelope without established nominal coverage; an `unsure`-as-wrong sensitivity is reported. Still no threshold recommended, no serving threshold changed. [SELECTIVE_RISK_REPORT.md](https://github.com/gasyoun/kosha/blob/main/data/concordance/selective_risk/SELECTIVE_RISK_REPORT.md)
