- **H4710 (OxAlpha `opencode/z-ai/glm-5.3-flash`) — the `dcs-sintagmatic-appendix6-periods` frequency-layer cross-check landed: Leonchenko's per-period collocate cores joined (IAST→SLP1 via [`sanskrit-util`](https://github.com/sanskrit-lexicon/sanskrit-util), 19,073/19,083 rows, 0 transcode failures) against the per-period `periods` vectors of [`lemma_frequency.tsv`](data/frequency/lemma_frequency.tsv).** Period mapping proven from the Leonchenko study itself (Table 1 core sizes 1,932/2,334/2,327/3,428/3,096/3,493/2,473 match the A6 line counts exactly). Verdict: buckets `1 -800`/`3200`/`4700`/`5 1200` independently confirmed (ρ 0.79–0.89, ≥84 % of lemmas within ±2×); buckets `2 -300`/`6 1700`/`7 1900` diverge (Epic/Rasaśāstra-scale A6 counts vs thin TSV slots — re-dating/extraction drift, observed as delta; systematic scale vs `count_all` baseline: median ratio 1.396). Fidelity finding: 723 lemmas carry duplicate rows inside A6 files (768 extra rows — sum before use). `lemma_frequency.tsv` bytes unchanged and stays authoritative. Artifacts: [`scripts/xcheck_appendix6_period_freq.py`](scripts/xcheck_appendix6_period_freq.py) (selftest PASS; frozen-sample verification 21/21 via in-script independent re-parse + 19/21 awk spot-check with the 2 misses being the duplicate-row artifact), [`data/frequency/appendix6_xcheck_delta.tsv`](data/frequency/appendix6_xcheck_delta.tsv), [`data/frequency/appendix6_xcheck_sample_frozen.tsv`](data/frequency/appendix6_xcheck_sample_frozen.tsv), report [docs/XCHECK_A6_PERIOD_CORES_LEMMA_FREQUENCY_15.09.26.md](docs/XCHECK_A6_PERIOD_CORES_LEMMA_FREQUENCY_15.09.26.md); `datasets.json` first consumer promoted (kosha, confidence high; Sanskrit-in-Numbers candidate retained). Consumer-candidate wiring from H3596.
