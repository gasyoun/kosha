- **H5273 (Claude Opus 5.5 `claude-opus-5-5`): a ŚKDR root (dhātu) entry no longer attaches to a PWG sense that PWG marks non-verbal. On the H5252 deck, 33 of 35 wrong matches are gone and none of the 10 correct or unsure matches is lost; card C013 *dhvaj* is kept.** H5252 measured the ŚKDR column at 77.8 % wrong, and most of those errors were one shape: PWG prints `ŚKDR.` under a noun, and ŚKDR's entry for the same string is a Kavikalpadruma verb root. The build now applies a gate in [app/sense_align.py](https://github.com/gasyoun/kosha/blob/main/app/sense_align.py) (`root_vs_nominal`, `pwg_pos`). A ŚKDR sense that H5252's dhātupāṭha lens reads as a root entry is never a candidate partner for a non-verbal PWG sense. That lens is `DHATU_MARKERS`, moved out of the scorer so the build and the scorer share one regex. PWG's own grammar decides the PWG side. Every PWG `<lex>` value is non-verbal, and root entries carry none. Under a `<lex>`, a gloss that is a German infinitive still counts as verbal: this is how C013 survives, since PWG glosses *dhvaja*'s root as *hinundherbewegen*. For addendum entries without a `<lex>`, a capitalised or article-led German gloss counts as nominal. No constant moved (TAU, GLOSS_FLOOR and PREFIX_MIN are unchanged). A withheld sense carries the new `root-vs-nominal` failure class.

  Rebuild on the H4751 W2 frame, with the 1,500 heads taken verbatim, homonym duplicates included. The pre-gate table was first reproduced byte for byte, except for one MW gloss line (`nI` mw:111189:3) that differs only because the local kosha.db has drifted; that line keeps main's bytes. Results:
  - ŚKDR-bearing aligned rows: 160 → 60.
  - PWG↔ŚKDR sense pairs: 105 removed, 8 added. The added pairs are greedy re-pairings, not adjudicated. On my reading, 4 look right (*ava*, *su* and *hi* avyaya records now meet PWG indeclinables, and *bhū* is verbal on both sides) and 4 look wrong (*mā* "Maass", *aṅga* "Geist (?)", *bhū* "Jmd umgehen", *mūla* "eigen", the last one being the adjective-in-*-en* residual).
  - Aligned rows without a ŚKDR cell: the 16 "new" ones are old rows that only lost their ŚKDR cell. No western partner changed.
  - The 2 wrong deck matches that survive, C035 *pari* and C043 *antareṇa*, are other-homonym cases without a dhātu mark, so they are outside this gate.
  - The lens was written after reading the H5252 deck, so these deck numbers are in-sample.
  - Codex Astra's first review re-derived every count above and they all matched. It returned FAIL on one classifier defect: a `ge-` participle exclusion also caught true infinitives (*geben*, *gehen*, *gewinnen*), so a verbal match under a `<lex>` could be gated. The exclusion is withdrawn. A comma-listed verb gloss (*halten, tragen*) is now read by its first item. Both changes err toward keeping an attachment, and the rebuilt table is byte-identical to the one reviewed.

  To re-derive the counts, run [scripts/measure_skd_root_nominal_gate.py](https://github.com/gasyoun/kosha/blob/main/scripts/measure_skd_root_nominal_gate.py) `--before-rev <pre-H5273 main>`. It only re-scores the frozen verdict files.

  The H5070 and H5252 evidence directories are byte-identical. Their reproduction tests now re-draw the decks from the frozen pre-H5273 table (git blob `4d08aa34`, sha256 pinned by both freezes) and skip on a shallow clone. Tests: `tests/test_sense_alignment.py` + `tests/test_sense_alignment_selective_risk.py` pass, 61 tests. Build report: [data/concordance/SENSE_ALIGNMENT_BUILD_REPORT.md](https://github.com/gasyoun/kosha/blob/main/data/concordance/SENSE_ALIGNMENT_BUILD_REPORT.md). The manifest row `sense-alignment-pilot` is refreshed.
