- **H4407 (OxAlpha `opencode/z-ai/glm-5.3-flash`) — the DB-build pipeline stops materializing whole tables in memory, the full-card tarball resumes after a kill instead of restarting from zero, and dead builds' scratch files get swept.** `scripts/build_entries.py` no longer `fetchall()`s an entire 86MB dictionary before inserting — it streams the source cursor in `fetchmany` batches and segments senses in the same pass off the row just written, rather than re-querying `entries` a second time; `scripts/build_forms.py` and `scripts/build_db.py`'s `build_lemmas`/`build_heritage` pass generators straight to `executemany` instead of building 400k–1.43M-row Python lists first. `scripts/build_static_cache.py`'s `build_full_tarball` (the opt-in ~682MB/222k-lemma release asset) now checkpoints: it stages into an uncompressed `*.stage` tar plus a `*.done` sidecar recording `<byte-offset>\t<slp1>` after every durably-flushed card, gzip-compressing to the real target only once every card is present. Resuming does **not** use `tarfile`'s own append mode — that mode scans for the standard end-of-archive trailer, which `TarFile.__exit__` skips writing on any exception, so a real `kill -9` never leaves one; resume instead seeks the raw stage file to the last recorded offset, truncates there (dropping the one torn in-flight card), and reopens in `mode="w"` against the pre-positioned file. `src/kosha/build/dag.py` gained `sweep_stale_build_tmp()`, run at the top of every `_run()`: `.build/*.<pid>.tmp[-journal]` scratch files whose PID is no longer alive are removed (a portable liveness check, no `psutil`); a live PID's tmp is left alone. Verified: full pytest suite green (688 passed, 0 regressions) against a freshly-built `kosha_fixture.db`; a dedicated resume test simulates a kill mid-tarball-build (exception raised inside `lemma_card` after 2/7 fixture cards) and confirms the resumed run stages exactly the remaining cards with no duplicate or missing members; two new GC-sweep tests cover a dead-PID tmp being removed and a live-PID tmp (this process's own) surviving. **Residual, out of scope this pass (stop-budget, [H4407](https://github.com/gasyoun/Uprava/blob/main/handoffs/H4407-OxAlpha_kosha_kosha-db-build-streaming-resume-gc_08.09.26.md)):** the `build_full_tarball` N+1 pre-join (`lemma_card()` per lemma still runs several queries each; only checkpointing/resume was in scope), and the fetchall-then-Python-limit patterns in `scripts/compare_vidyut_cologne.py`, `scripts/build_paradigms.py`, `scripts/build_evidence.py`. Peak-RSS memory profiling and a pre/post row-count parity run against the real `mw` dictionary were not done — this box has no local `data/db/kosha.db` (real dict data needs a `gh release download` from `csl-sqlite`, out of the ~45-minute unattended budget); correctness was instead verified functionally against the 7-lemma fixture pack (`build_db.py --profile fixture`, all ten stages, stable across two clean rebuilds).
