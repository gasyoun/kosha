- **H5070 (Claude Opus 5 `claude-opus-5`) — selective risk and abstention measured on the scaled aligned-sense table; the frozen W2 acceptance sample no longer matches the population it names.** H4751 scaled the population and H4745 then extended it with the MD channel; the recorded coverage increase and the reproduced judge κ were not an error-versus-coverage curve for the enlarged table (κ is a judge agreeing with itself, on a population two waves old). This wave freezes the population by hash and adjudicates a bounded new deck against it. **Provenance defect found and characterised:** the W2 acceptance sample reproduces **byte-for-byte from its own revision `bd758f0f0`**, but not at HEAD — H4745 (`e98910b6f`) rebuilt the table underneath it (aligned 7,009 → 7,087, sample schema 25 → 27 columns), so the frozen rule now selects 155 cards of which only **122 of the frozen 152 survive** (30 re-drawn, 33 new; `SAKa#1` moved 0.533 → 0.600 and changed stratum). **All 30 dropped cards remain in the population — nothing was deleted**; the seeded draw moved because stratum sizes moved. The frozen sample stays a valid sample of the pre-MD population and an invalid one of the current table. **Measurement:** 60 newly adjudicated pairs (219 reused-gold cards excluded from selection so nothing is double-counted), equal budget per score band, stratified estimates re-weighted to population — strict wrong-match 20.0 % (95 % CI 10.0–35.4) over all aligned rows, 18.4 % at score ≥ 0.40, 12.6 % (0.3–36.0) at ≥ 0.70; lenient counterparts 10.1 / 10.2 / 7.4 % excluding near-miss verdicts (metonymy, POS variant, sibling referent). The point estimate falls monotonically with confidence, but the intervals overlap completely — **calibration is INCONCLUSIVE at n = 60 and no threshold is recommended; no serving threshold changed**. Acceptance precision stays behind the family's human-vote fence; this is an agent-adjudicated risk estimate, not that figure. **Abstention reported separately** (never folded into error): 60,143 of 67,230 candidate groups declined (89.46 %), with `absent-dictionary` (492) split out as the unreachable-pair class that is neither match nor miss. **Controls:** a blind seeded confident wrong match (`rajas#9` — PWG *rajas* 'dust' paired with MW/Apte *saṃghāta* 'heap', dressed at 0.930, identity sealed in `canary_key.json` until verdicts were written) was caught as `different` — PASS, and is excluded from every rate; deck and canary reproduce byte-for-byte. New: [scripts/sample_sense_alignment_selective_risk.py](https://github.com/gasyoun/kosha/blob/main/scripts/sample_sense_alignment_selective_risk.py), [scripts/score_sense_alignment_selective_risk.py](https://github.com/gasyoun/kosha/blob/main/scripts/score_sense_alignment_selective_risk.py), [scripts/render_selective_risk_deck.py](https://github.com/gasyoun/kosha/blob/main/scripts/render_selective_risk_deck.py), [data/concordance/selective_risk/](https://github.com/gasyoun/kosha/blob/main/data/concordance/selective_risk/) (deck, freeze, verdicts, canary key, [SELECTIVE_RISK_REPORT.md](https://github.com/gasyoun/kosha/blob/main/data/concordance/selective_risk/SELECTIVE_RISK_REPORT.md)), 9 tests in [tests/test_sense_alignment_selective_risk.py](https://github.com/gasyoun/kosha/blob/main/tests/test_sense_alignment_selective_risk.py). Frozen H3910/W2 artifacts and aligner constants untouched. `SENSE_ALIGNMENT_BUILD_REPORT.md` is machine-generated (`build_sense_alignment.py` rewrites it whole), so it was NOT hand-edited: the pointer to this evidence was added to the generator instead and lands in the report at its next legitimate rebuild — regenerating it now would need the 1.7 GB gitignored `kosha.db` and a full re-align, which this handoff's scope forbids.
