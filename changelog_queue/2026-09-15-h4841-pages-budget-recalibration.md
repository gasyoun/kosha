- **H4841 (Opus 5 `claude-opus-5`, OxAlpha-tier handoff) — the W4b Pages-budget gate now measures what Pages actually publishes, and reads GATE FAIL at 144.5%.** [`measure_pages_budget.py`](scripts/measure_pages_budget.py) sums tracked blob sizes at HEAD by tier (Pages source is legacy `main:/`), so the committed `w/` (417.7 MB) and `data/` (571.7 MB) are counted: **1,479.9 MB published**, served tiers alone 876.9 MB = 85.6%. Root causes of the old PASS: the sampler rendered pages without the published ux layer (22.80 vs 36.75 KB/page) and multiplied by N=11,148 instead of the 10,370 head lemmas with a card. The new stratified frame (take-all top 2% of cards + size-sorted head/mid/tail bands) lands **+0.37%** from the byte-true census of the committed head. The 70% ceiling is unchanged. The headroom plan recommends serving only an allowlist via an Actions workflow (−603 MB, no re-render), pending MG's go: [PAGES_HEADROOM_PLAN_H4841_15.09.26.md](docs/PAGES_HEADROOM_PLAN_H4841_15.09.26.md).
