- **H4038 (OxAlpha `zai-coding-plan/glm-5.3-flash`) — Hitopadeśa per-text concordance surfaced with era badges on the reading-pack page (MG «да» 04-09-2026 on «выводить ли конкорданс Хитопадешы на поверхность с бейджами»), extending the H4034 pilot onto the H1448 reader surface without creating a parallel one.** The fold page now renders through the H4026 machinery verbatim, never a second render system: the page-level work badge is `app/dating_hydrate.py::badge_html` output (early-medieval via dharmamitra, EN+RU tooltip caveat), the RU+EN caveat + five-bucket legend is `app/word_page.py::_dating_caveat_block` byte-for-byte (the same wording every H4026-badged word page carries), and the era CSS is ported from PAGE_CSS (`data-era`-keyed, all 5 buckets). Wiring: selecting `hitopadesa-0` on [reading/index.html](https://github.com/gasyoun/kosha/blob/main/reading/index.html) reveals a «Word concordance →» affordance (additive conditional, every other pack's view unchanged); the concordance page links back to the pack view. Gates shipped as `scripts/build_text_concordance_hitopadesa.py --check` (DCS-free, also run in-build) + [tests/test_text_concordance_hitopadesa.py](https://github.com/gasyoun/kosha/blob/main/tests/test_text_concordance_hitopadesa.py) (8 tests): **parity** of concordance.tsv vs the rendered viewer payload vs page stats vs MANIFEST (byte re-derivation, 7,857 rows / 25,040 occurrences / 13.0% sense-linked), **order-invariance** of the underlying text (payload canonicalized by the build's own sort key; every row's refs non-decreasing in chapter→sentence→subcounter document order), and the **honest-absence contract** (no work bucket ⇒ no badge, no caveat, gate-fails if rendered). **Additive fence held**: a full DCS rebuild left `concordance.tsv` and `text_hitopadesa.js` byte-identical (sha256 pre/post) — and the new gate caught + fixed a latent H4034 drift where the committed script no longer reproduced the committed js (payload row key order `n_refs`/`refs` swapped between artifact and script); the script now regenerates both committed artifacts byte-identically. Rendered proof committed: [data/concordance/text_hitopadesa/evidence/H4038_SURFACE_PROOF.md](https://github.com/gasyoun/kosha/blob/main/data/concordance/text_hitopadesa/evidence/H4038_SURFACE_PROOF.md) + live-shaped `badges-surface.hitopadesa.html` (the `api` row: 464 occurrences in document order, 13 sense-id chips, `/w/api.html` card, under the early-medieval badge; the `avalokya` row — unlinked causative `-ay` stem — honestly `—`). Live-serve smoke: reader page, concordance page, js shard and proof all HTTP 200 at the repo-relative layout; production deploy stays human-gated per [KOSHA_DEPLOYMENT.md](https://github.com/gasyoun/kosha/blob/main/KOSHA_DEPLOYMENT.md). Full fixture suite green (472 passed / 37 skipped + the 8 new + test_sense_dating 17/17).
