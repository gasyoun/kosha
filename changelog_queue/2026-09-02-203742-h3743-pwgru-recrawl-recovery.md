- **H3743 (Sonnet 5 `claude-sonnet-5`) — pwg_ru corpus recovery: a `--dict` filter + a generalized self-healing watchdog, unblocking the akshara full-corpus benchmark after its input was lost to a premature worktree GC ([Uprava FINDINGS §660](https://github.com/gasyoun/Uprava/blob/main/FINDINGS.md#660)).** [scripts/akshara_full_crawl.py](https://github.com/gasyoun/kosha/blob/main/scripts/akshara_full_crawl.py) gained `--dict {mw_ru,apte_ru,pwg_ru}` so a targeted recrawl re-fetches only the missing MT variant instead of tripling the volume for no reason, and `--log` now applies to the `--ru` pass too so a recrawl writes its own manifest instead of colliding with the historical one. New [scripts/akshara_recrawl_watchdog.ps1](https://github.com/gasyoun/kosha/blob/main/scripts/akshara_recrawl_watchdog.ps1) generalizes H3597's hardcoded two-pass watchdog into a single-dict/single-log supervisor matched on its `--log` argument, registered as a 10-min scheduled task with `StartWhenAvailable`/`WakeToRun`. Re-crawl of `pwg_ru` over the full 51,663-head census launched (~20h ETA); once complete the parsed corpus will be archived to `D:\ClaudeTools\evidence\` before any worktree GC, per the same rule this incident exists to enforce.
