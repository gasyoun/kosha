- **H4732 (OxAlpha `opencode/z-ai/glm-5.3-flash`) — headword-overlap-matrix extended with the Tamil fold: 15 → 18 dicts, 105 → 153 cells.** Census C5 lands [headword_overlap_matrix_ext.tsv](data/tamil/headword_overlap_matrix_ext.tsv) — the registered 15-dict matrix (H684) predates the 06-09 [tamil-fold](data/tamil/fold_stats.json) ingest (H4178), so **mwd / cap / otl** join as new codes: 48 new cells (3×15 vs the union + 3 among the fold), cpd/Pahlavi excluded exactly as csl-santam's own UI excludes it. Keying: fold `st` is Kyoto-Harvard (mwd/cap) or HK-like (otl) → house chain `scheme_bridge.hk_to_iast` × `sanskrit_util.to_slp1` (SLP1 table imported, never re-typed); minimal `&`-variant split, `-`-continuation drop, residue counted. **Extension-invariance PASS** — the 105 base cells are identical recomputed with and without the fold (adding codes adds rows/columns only); the 27 cells that differ from the H684 baseline (≤3 union size) come from the 04-09 H4075 union regen (323,425→323,422), recorded per cell in [overlap_matrix_ext_stats.json](data/tamil/overlap_matrix_ext_stats.json), not caused by this extension. Canaries validate the chain: **cap × CAE J = 0.7957**, **mwd × MW J = 0.7838**; otl contributes its Sanskrit layer (1,593 keys into the union, 913 shared with MW; 32.5% expected Tamil-phonology residue). Nothing registered changes — the 15-dict dataset, union master and HeadwordLists are untouched. Delta report: [data/tamil/OVERLAP_MATRIX_EXT_DELTA_14-09-2026.md](data/tamil/OVERLAP_MATRIX_EXT_DELTA_14-09-2026.md); data statement: [docs/data-statements/headword-overlap-matrix-ext.meta.md](docs/data-statements/headword-overlap-matrix-ext.meta.md); builder: [scripts/build_overlap_matrix_ext.py](scripts/build_overlap_matrix_ext.py) (`--verify` PASS, byte-identical rerun).
