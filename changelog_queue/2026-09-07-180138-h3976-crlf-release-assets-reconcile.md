- **H3976 (Sonnet 5 `claude-sonnet-5`) — the three DOI-archived data releases' CRLF-inflated bytes are reconciled, without touching an already-minted Zenodo deposit.** [FINDINGS §654](https://github.com/gasyoun/Uprava/blob/main/FINDINGS.md#654) (H3788) proved the manifest fix but left the published GitHub release assets untouched; re-derived independently here from the live `gh release download` bytes, **nine assets** across `data-v0.2.0`/`data-v0.3.0`/`data-v0.5.0` still carry CRLF (delta == line count in every case, LF-canonical sha256 matches the committed frozen manifest for all nine). Zenodo mints one immutable deposit per pushed tag and never re-archives an asset swapped under an existing tag, so re-uploading would desync the GitHub copy from the DOI snapshot instead of fixing anything — recorded instead in [docs/ERRATA_CRLF_RELEASE_ASSETS_07-09-2026.md](https://github.com/gasyoun/kosha/blob/main/docs/ERRATA_CRLF_RELEASE_ASSETS_07-09-2026.md) with both byte sequences and the reproducible LF-canonical checksum per file. One manifest row (`mw-heritage-crosswalk`) had never actually been repaired despite the earlier repaired-count claim — fixed (`size_bytes` 3,263,954 → 3,078,150). Renormalize confirmed clean on this Mac's canonical checkout (0 changes); other long-lived checkouts unverified. Two release-cutting scripts (`migrate_manifest_schema.py`, `cut_data_v020.py`) wrote the manifest without `newline="\\n"` and would have reintroduced CRLF on a Windows re-run — both pinned. `freeze_release_manifest.py`'s LF-canonical checksum gate (H3788) already covers the mission's assert-checksums-over-canonical-bytes requirement; no further gate wiring was in scope here.
