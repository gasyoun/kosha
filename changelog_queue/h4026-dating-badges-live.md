- **H4026 (OxAlpha `zai-coding-plan/glm-5.3-flash`) — sense-dating era badges LIVE on the kosha surface (MG order 03-09-2026: «показать бейджи на поверхности kośa»), unparking the H4019 P3 staged render.** Three defects stood between the staged pass and a meaningful live render, all fixed here. (1) **Ordering/coverage**: `app/dating_hydrate.py` matched only the plain `<span class='ls'>` emission, but on the live path [app/ls_hydrate.py](https://github.com/gasyoun/kosha/blob/main/app/ls_hydrate.py) has already rewritten every resolvable citation into an `<a class="ls ls-scan|ls-etext">` link — the staged pass badged ONLY the citations ls_hydrate could NOT link (on padma sense 4, whose sole citation `VARĀH. BṚH. S. 52,29` resolves to a BṚhatsaṃhitā scan link, that meant zero badges). The badge pass now covers both markup forms — badge appended inside the span, inside the anchor (riding the citation's own link) — and adds the continuation-citation `title` fallback (`<ls n="ṚV. 4,">22,9</ls>` keeps its abbreviation in the title; a coordinate-only title like the ls `n`-attribute form `title='7'` is never an abbreviation). Linked continuations, whose abbreviation survives only in the URL after the ls_hydrate rewrite, stay honest misses — the pass never reverse-resolves a link URL (never a second resolver). (2) **Publication wiring**: the explicit `ux["sense_dating"]` key stays (the H3744 doctrine — the no-ux render path is byte-identical, still locked), but the live build now sets it: `scripts/build_word_pages.py`'s default `ux` is the published layer `{"variant": "a", "sense_dating": True}` — which also closes a latent trap: since the H3457 publish (26-08) the committed `w/` tree carried organs the build default would not have rendered, so a blind rebuild would have silently stripped them. The committed `w/` tree (2,324 pages) is regenerated `--reading-packs --force`: **1,818 pages carry ≥1 badge**, organs byte-identical to the H3457 publish, badge CSS + bucket legend added to `PAGE_CSS`. (3) **The caveat contract**: the page renders the RU+EN preface caveat as a `dating-note` disclosure whenever ≥1 badge rendered (hits threaded from `_entry_html` through the per-render ux dict) — «Первое засвидетельствование значения в цитируемом корпусе — не происхождение значения» / *first attestation of a meaning in the cited corpus, not the origin of the meaning* — plus the five-bucket legend; disputed sources are named as never dated. **Honesty unchanged**: disputed works still refuse badges by omission (Suśruta: all 31 `SUŚR.` citations on the han page badge-free), homonym-dense abbreviations still never badge, printed PWG order untouched (order-invariance and link-structure asserts added). **Rendered proof committed**: [data/dating/evidence/H4026_BADGES_LIVE_PROOF.md](https://github.com/gasyoun/kosha/blob/main/data/dating/evidence/H4026_BADGES_LIVE_PROOF.md) + live-shaped `badges-live.padma.html` (sense 4〉 pillar-part and 5〉 temple-form visibly `classical`) and `badges-live.han.html` (1,162 badges, vedic floor present, Suśruta refusals). Gates: `build_sense_dating.py --check` parity OK (11,347 works / 278 abbrevs / 7,349 senses); `tests/test_sense_dating.py` **17/17** (was 11/11 — six H4026 render gates added: anchor badge with href+inner preserved, anchor idempotency, continuation-title fallback, disputed-refuse in both forms, padma live render + caveat, han verb honesty); full suite 619 passed on the workstation after installing the two pinned deps that were missing on this box (`sanskrit-util` at the requirements.txt-pinned commit, `vidyut>=0.4.0` from PyPI — the canonical install doc `pip install -r requirements.txt` was already correct; only the box env lagged). **Scope note (measured)**: the surface badge remains per-citation by design — a SENSE-keyed render of `sense_dating.tsv.first_era` would need a hom→entry mapping the surface does not have (185/255 layer headwords with a committed card have multiple PWG entries per slp1; printed-marker fingerprints match several entries at once), so it would silently attach eras to the wrong homonym; the per-citation badge keys on the citation's own abbreviation and cannot mis-attach. The H4019 record already parked per-sense labels for the same anchor reason; this pass adds the collision measurement to that fence in [data/dating/README.md](https://github.com/gasyoun/kosha/blob/main/data/dating/README.md).
