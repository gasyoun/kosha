- **H4729 (OxAlpha `opencode/z-ai/glm-5.3-flash`) — unified upasarga atlas: the three upasarga datasets become one dataset.** Census C2 three-way join lands [upasarga_atlas.tsv](data/upasarga/upasarga_atlas.tsv) (13,436 rows) — one `(root, preverb)` key per row across the DCS prefixed-verb decomposition ([dcs-upasarga-prefixus](data/upasarga/dcs_upasarga_prefixus.tsv), keyed on the resolved `prefix` column only — the naive `prefix_guess` never keys; 1,366 no-prefix rows collapse to root-level keys), the Gita root × preverb semantics ([sanskrit-upasarga-semantics](data/gita/upasarga_semantics.tsv), preverbs verbatim, comma-lists not exploded) and the VisualDCS per-class verb-form frequencies (root level only, non-zero classes as `1:30478|3:22`). Shared nonempty-preverb keys prefixus × semantics: **34 — the H4477 census anchor reproduced exactly**; census shape: classfreq-only 7,020 / prefixus-only 5,037 / prefixus+classfreq 1,165 / all-three 108 / semantics-only 52 / prefixus+semantics 34 / semantics+classfreq 20. `--verify` mode cross-checks the emitted TSV (key-set parity, H4477 anchor, deterministic 5-key parity sample, full classfreq round-trip); rerun byte-identical (sha256 `97f3b975…`). First consumer of `dcs-upasarga-prefixus`, second of `sanskrit-upasarga-semantics` and `dcs-verb-class-prefix-frequency` (zero-consumer status retired on the latter). CC BY-SA 4.0, credit Dr. Mārcis Gasūns. Data statement: [docs/data-statements/upasarga-atlas.meta.md](docs/data-statements/upasarga-atlas.meta.md); builder: [scripts/build_upasarga_atlas.py](scripts/build_upasarga_atlas.py).
